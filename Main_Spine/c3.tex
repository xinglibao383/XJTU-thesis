% !TeX root = ../main.tex

\xchapter{基于多模态融合的人体活动识别}{Human Activity Recognition Based on Multimodal Fusion}

本章针对基于多模态融合的人体活动识别问题展开研究。在前述相关理论与技术基础上，第3.1节首先对当前人体活动识别领域面临的关键问题进行分析，重点讨论单一传感模态在复杂环境中的局限性以及多模态感知的必要性。第3.2节介绍实验数据集的构建过程，包括数据采集的硬件配置、场景设置以及数据预处理方法。第3.3节对本文提出的人体活动识别方法进行系统阐述，包括整体方法框架及各关键模块的实现思路。第3.4节通过实验对所提出方法进行分析与评估，从多个角度验证其有效性与鲁棒性。最后，第3.5节对本章的研究内容进行总结。

\xsection{问题描述}{Problem Description}

随着物联网基础设施的不断完善以及人工智能技术的发展，人体活动识别（Human Activity Recognition, HAR）逐渐成为连接物理世界与数字空间的重要技术，在智慧医疗、养老监护和安防监控等领域具有广泛应用。然而，构建高精度、全天候且具备良好鲁棒性的感知系统仍然面临诸多挑战，包括感知手段受限、异构数据特征难以对齐以及任务建模方式较为单一等问题。现有主流解决方案，无论是基于视觉信息的方法还是基于单一非视觉传感器的方法，在复杂现实场景中均存在一定局限。

首先，基于图像或视频的视觉感知技术在理想环境下能够获得较好的识别效果，但在实际应用中仍面临多方面限制。例如，该类方法对光照条件具有较强依赖，在夜间或光线较弱的环境中成像质量明显下降，从而影响特征提取效果。此外，家具或墙体遮挡也可能导致人体关键点检测失败，降低系统识别稳定性。更重要的是，持续的视频采集在隐私敏感场景中往往难以被用户接受，尤其是在卧室等私密环境中。因此，探索非视觉、低成本且隐私友好的感知方式逐渐成为研究热点。

其次，惯性测量单元（IMU）与WiFi感知作为两种典型的非视觉感知方式，虽然在一定程度上规避了视觉方案的隐私与光照问题，但单一模态仍存在明显局限。IMU属于接触式传感器，能够高频采集人体局部运动的加速度与角速度信息，对细粒度动作变化具有较高敏感性，但其缺乏对环境空间信息的感知能力，难以区分动作模式相似但发生场景不同的活动。同时，低成本MEMS传感器在长时间监测过程中容易产生零偏漂移，从而影响测量稳定性。相比之下，WiFi感知通过分析信道状态信息（CSI）反映人体对电磁波传播的扰动特征，具有非接触和覆盖范围广等优势，但其信号对环境多径变化较为敏感，环境布局变化可能导致特征分布发生偏移。因此，IMU与WiFi在物理属性与数据分布上存在显著差异，简单的特征拼接往往难以实现有效融合。

此外，现有研究大多将人体活动识别视为单一分类任务，而较少考虑身份与场景等相关语义信息。在实际应用中，用户行为通常由“谁（Identity）”“在何处（Scene）”以及“做什么（Activity）”等多个因素共同描述。将这些信息割裂处理不仅难以充分利用任务之间的潜在关联，也可能限制模型对复杂行为模式的理解能力。

针对上述问题，本文提出一种基于多模态融合的人体活动识别方法WICTIVITY。该方法利用IMU与WiFi信号在感知机理上的互补性，通过双分支时序编码提取不同模态特征，并利用跨模态对比学习策略实现异构特征在潜在空间中的对齐。在此基础上，通过多模态特征融合构建统一的表示，并结合多任务学习框架同时完成活动识别、身份识别与场景识别任务，从而提升系统在复杂环境中的识别性能与泛化能力。

\xsection{数据集构建}{Dataset Construction}

\xsubsection{XRFV2数据集}{XRFV2 Dataset}

本文实验基于多模态人体感知数据集XRFV2开展。该数据集通过同步采集WiFi CSI与IMU数据，记录人体在真实室内环境中的日常行为活动，为多模态人体行为理解研究提供数据基础。数据采集在三个典型室内生活场景中进行，包括餐厅、书房以及卧室。不同场景具有不同的空间布局与环境结构，用于模拟日常生活中的多种活动环境。实验场景以及WiFi设备部署方式如图3-1所示。

在每个场景中均部署一套WiFi感知系统，包括一台发射器（Tx）和三台接收器（Rx），用于采集CSI。发射器采用单天线，在128信道（5.64\,GHz）以约200 packets/s 的速率持续发送数据包；每个接收器配备三根天线，用于接收无线信号。在数据采集过程中，通过WiFi CSI Tool从30个OFDM子载波中提取CSI数据。对于每个时间步，CSI数据由发射天线与接收天线之间的多输入多输出（MIMO）信道测量构成，可表示为一个四维张量结构，其维度为 $1 \times 3 \times 3 \times 30$。随着时间序列不断累积，最终形成一个随时间变化的CSI序列，用于刻画人体运动对无线传播环境产生的动态扰动。

\begin{figure}[h]
  \centering
  \includegraphics[width=\textwidth]{数据集采集场景.png}
  \caption{数据集采集场景}
\end{figure}

除了WiFi信号外，数据集还同步采集人体惯性传感器数据。参与实验的志愿者佩戴多个IMU设备，包括左右口袋中的两部智能手机、左右手腕佩戴的两只智能手表以及一副智能眼镜，共形成五个IMU节点。IMU佩戴位置示意如图3-2所示。对于每个IMU节点，传感器在每个时间步均输出三轴加速度和三轴角速度数据，因此每个节点对应6维惯性测量信号。当五个IMU设备同时工作时，可以获得一个随时间变化的多节点惯性数据序列，其整体结构可表示为 $T \times 5 \times 6$，其中 $T$ 表示时间序列长度，5表示IMU设备数量，6表示每个设备的传感器通道数（3轴加速度与3轴角速度）。

\begin{figure}[h]
  \centering
  \includegraphics[width=\textwidth]{IMU佩戴位置示意图.png}
  \caption{IMU佩戴位置示意图}
\end{figure}

数据集共包含16名志愿者在不同场景下完成的30类日常动作，共采集853段连续动作序列，总时长约16小时。不同场景中包含的具体动作类别如表3-1所示。

\begin{table}[H]
  \centering
  \caption{场景动作类别}
  \begin{tblr}{
    colspec={X[1,c] X[7,c]},
  }
  \toprule
  场景 & 动作 \\
  \midrule

  餐厅 & 走路、坐下、站起来、往杯子里倒水、喝水、吃药、捡起物品、从柜子里拿水果、切水果、吃水果、洗手、扔垃圾、擦桌子、坐着时伸展 \\
  书房 & 走路、坐下、站起来、往杯子里倒水、喝水、吃药、打开和关闭台灯、操作鼠标、写字、操作键盘、读书、打开信封、接电话、站立时伸展、在黑板上写字 \\
  卧室 & 走路、起床、坐下、躺下、站起来、读书、往杯子里倒水、喝水、吃药、站立时伸展、使用手机、打开和关闭窗户、打开和关闭窗帘、给植物浇水、站立不动、躺着不动 \\

  \bottomrule
  \end{tblr}
\end{table}

\xsubsection{数据处理与数据划分}{Data Processing and Dataset Split}

为了将连续采集的多模态传感器数据转换为适用于模型训练的输入样本，需要对原始时间序列进行分割与整理。

本文采用滑动窗口方法对连续数据进行切分，即在时间序列上按照固定长度的窗口提取局部片段，并通过移动窗口获得多个样本。例如，当一个时间窗口覆盖某一段连续动作序列时，该窗口所对应的数据片段即可作为一个训练样本。在窗口分割过程中，部分时间窗口可能同时包含多个动作片段。为保证标签的一致性，本文采用基于持续时间的标签分配策略，即将窗口中持续时间占比最大的动作类别作为该样本的标签。通过这种方式，可以在保持时间序列连续性的同时获得较为稳定的样本标签。

完成样本构建后，数据集进一步划分为训练集与测试集。其中训练集用于模型参数学习，测试集用于评估模型性能。

\xsection{方法描述}{References}

\xsubsection{方法整体流程}{Ref equations}

本研究提出的基于多模态融合的人体活动识别方法（WICTIVITY），其整体实施流程如图3-4所示。该流程构建了一个从物理世界的异构信号感知到数字世界的语义特征融合，再到多维状态联合推断的端到端系统，主要包含三个核心阶段：多模态数据采集、异构信号处理以及多任务协同推理。

\begin{figure}[h]
  \centering
  \includegraphics[width=\textwidth]{人体活动识别方法整体流程.png}
  \caption{人体活动识别方法整体流程图}
\end{figure}

在数据采集阶段，系统旨在构建一个全方位、多视角的智能感知环境。为了捕捉人体活动的细微变化与宏观状态，研究同步部署了接触式与非接触式两类传感器。一方面，利用佩戴于用户的惯性测量单元（IMU）实时采集高频的三轴加速度与角速度数据，这些数据能够精确反映肢体末端的运动学特征与瞬态变化；另一方面，利用商用Wi-Fi设备采集信道状态信息（CSI），通过分析射频信号在传播路径上的多径效应与多普勒频移，捕捉人体躯干运动对环境电磁场的扰动特征。这种多源异构数据的同步采集策略，充分利用了IMU对局部动作的敏感性与CSI对空间环境的感知力，为后续的全场景理解提供了互补的物理信息基础。

数据处理阶段的核心任务是消除原始信号中的环境噪声与硬件伪影，并将异构数据转化为适配深度神经网络输入的标准化张量。针对IMU数据，主要进行去噪、重力补偿与归一化处理，以消除传感器漂移与佩戴位置差异带来的影响；针对CSI数据，则需进行异常值剔除、子载波相位校准与幅度平滑，以提取纯净的动作特征。更为关键的是，由于两类传感器采样频率不同，本阶段还涉及严格的时间同步与滑动窗口切片操作，将连续的时间序列划分为包含多模态信息的对齐样本，从而为模型训练提供具有严格时序对应关系的输入数据。

最后是多任务协同推理阶段，这是本方法的核心计算环节。预处理后的多模态序列被输入到WICTIVITY深度学习模型中。模型首先利用双流Mamba编码器，以线性计算复杂度分别提取IMU与CSI信号中的长时序依赖特征；随后，引入基于CLIP机制的特征对齐模块，通过对比学习策略在潜在特征空间中拉近同源样本的距离，消除异构模态间的语义鸿沟；对齐后的特征经过级联融合，形成包含丰富时空信息的统一表征。最终，通过并行的多任务分类网络，模型在一次前向传播中同时输出用户的活动类别、身份信息与所处场景。在训练过程中，系统通过联合优化分类损失与对比损失，驱动网络学习具备强泛化能力的多模态特征表示，从而实现对“人-动作-环境”三元组关系的整体感知。

\xsubsection{WICTIVITY模型}{}

随着物联网技术的飞速发展与普适计算需求的日益增长，基于多模态传感器的人体感知技术已成为实现智能环境交互的关键。然而，现有的感知方法在处理惯性测量单元（IMU）与Wi-Fi信道状态信息（CSI）这两种异构模态数据时，面临着严峻的挑战。一方面，IMU数据作为一种接触式传感信号，能够精准捕捉肢体末端的加速度与角速度变化，具有极高的时间分辨率，但缺乏对整体空间环境的感知能力；另一方面，Wi-Fi CSI数据作为一种非接触式射频信号，虽然对环境多径效应与躯干运动敏感，但极易受到环境噪声干扰且难以捕捉细微的肢体动作。此外，这两种模态在物理属性、采样频率及特征分布上存在显著的“模态鸿沟”（Modality Gap），导致传统的直接拼接或简单融合策略难以在特征空间内实现深层的语义对齐。

针对上述问题，本文提出了一种基于状态空间模型与对比学习的多任务统一感知框架——WICTIVITY模型。该模型的设计初衷在于构建一个端到端的深度学习架构，能够在统一的特征空间内，协同利用IMU的局部精细感知能力与Wi-Fi CSI的全局空间感知能力，同时完成人体活动识别（Activity Recognition）、身份识别（Identity Recognition）和场景识别（Scene Recognition）三个核心任务。

WICTIVITY模型的整体架构如图3-4所示。在模型设计上，我们并未沿用传统的卷积神经网络（CNN）或Transformer作为骨干网络，而是创新性地引入了选择性状态空间模型（Selective State Space Model, Mamba）。相较于Transformer高达$O(L^2)$的二次方计算复杂度，Mamba凭借其线性时间复杂度$O(L)$的优势，能够更高效地处理长周期的多模态时序信号，精准捕捉人体动作在长时域内的演变规律。此外，为了解决异构特征的不对齐问题，我们在特征融合前引入了受CLIP（Contrastive Language-Image Pre-training）启发的特征对齐机制，通过自监督的对比学习策略，强制模型学习不同模态间的一致性语义表达。

从数学形式上定义，假设输入的多模态数据集合为 $\mathcal{D} = \{(\mathbf{X}_{IMU}^i, \mathbf{X}_{CSI}^i, \mathbf{Y}^i)\}_{i=1}^N$，其中 $N$ 表示样本总数。对于第 $i$ 个样本，$\mathbf{X}_{IMU}^i \in \mathbb{R}^{T \times C_I}$ 表示时间长度为 $T$、通道数为 $C_I$ 的IMU序列数据，$\mathbf{X}_{CSI}^i \in \mathbb{R}^{T \times C_W}$ 表示同时间长度下、子载波数量为 $C_W$ 的CSI序列数据。目标标签 $\mathbf{Y}^i = \{y_{act}^i, y_{id}^i, y_{scene}^i\}$ 分别对应活动类别、用户身份及场景类别。

WICTIVITY模型旨在学习一个非线性映射函数 $\mathcal{F}$，该函数由特征提取器 $\Phi$、特征对齐模块 $\mathcal{A}$、特征融合模块 $\mathcal{M}$ 以及多任务分类器 $\Psi$ 级联组成。其前向传播过程可形式化地表示为：

\begin{equation}
\begin{aligned}
\mathbf{Z}_{IMU} &= \Phi_{IMU}(\mathbf{X}_{IMU}; \theta_{I}), \quad \mathbf{Z}_{CSI} = \Phi_{CSI}(\mathbf{X}_{CSI}; \theta_{W}) \\
\tilde{\mathbf{Z}}_{IMU}, \tilde{\mathbf{Z}}_{CSI} &= \mathcal{A}(\mathbf{Z}_{IMU}, \mathbf{Z}_{CSI}; \theta_{align}) \\
\mathbf{V}_{fused} &= \mathcal{M}(\tilde{\mathbf{Z}}_{IMU}, \tilde{\mathbf{Z}}_{CSI}; \theta_{fuse}) \\
\hat{\mathbf{Y}} &= \Psi(\mathbf{V}_{fused}; \theta_{cls})
\end{aligned}
\end{equation}

其中，$\theta$ 代表各模块的可学习参数。$\mathbf{Z}_{IMU}$ 和 $\mathbf{Z}_{CSI}$ 分别为经过Mamba编码后的深层特征表示，$\tilde{\mathbf{Z}}$ 为经过对齐后的特征向量，$\mathbf{V}_{fused}$ 为多模态融合特征向量，$\hat{\mathbf{Y}}$ 为最终的预测结果。

本章节将详细阐述WICTIVITY模型的各个关键组成部分。首先介绍基于Mamba的Embedding模块，解析其如何高效提取长时序依赖特征；随后阐述特征对齐模块，探讨如何利用CLIP机制缩减模态间的语义差异；接着描述特征融合模块与多任务分类模块的具体实现；最后，定义模型训练所使用的复合损失函数，分析其在多任务协同优化中的作用。

\begin{figure}[h]
  \centering
  \includegraphics[width=\textwidth]{活动识别模型架构图.jpg}
  \caption{人体活动识别模型架构图}
\end{figure}

\subsubsection{Embedding模块}{}

Embedding 模块作为 WICTIVITY 模型的感知前端，核心功能是实现异构传感器信号的标准化空间映射与高维特征表征。由于惯性测量单元（IMU）提供的动力学特征与 WiFi 信道状态信息（CSI）提供的环境感知特征在物理属性及采样频率上存在显著差异，模型首先需要通过特征嵌入操作消除模态间的异构性，为后续的特征融合奠定基础。

具体的特征提取过程可以抽象为如下映射关系：
\begin{equation}
Z_{IMU} = \Phi_{IMU}(X_{IMU})
\end{equation}
\begin{equation}
Z_{CSI} = \Phi_{CSI}(X_{CSI})
\end{equation}
其中，$X_{IMU}$ 与 $X_{CSI}$ 分别代表原始的 IMU 序列与 WiFi CSI 序列，$\Phi$ 表示由线性投影层与多层状态空间模型构成的嵌入算子。

在实现细节上，模块首先利用线性投影层（Linear Projection）将异构输入的通道维度映射至统一的潜在特征维度 $D$。这一步骤在统一特征维度的同时，初步提取了不同物理通道间的局部关联性。为了增强模型对人体动作演进逻辑的解析能力，我们在嵌入特征中融入了位置编码，从而显式地引入时序方向信息。

模块的时序特征挖掘由双流 Mamba 编码器驱动。相较于传统的循环神经网络，该架构凭借选择性扫描机制，能够以线性计算复杂度高效捕捉长时域内的运动规律。通过多层级编码块的深度演化，模型能够从包含噪声的原始电信号中，逐层提取出具备平移不变性的运动模式特征。最终生成的特征向量 $Z_{IMU}$ 与 $Z_{CSI}$ 不远在维度上实现了对齐，更深度解构了人体活动的动力学纹理与环境电磁场的扰动特征，为后续的跨模态语义空间对齐提供了高质量的表征基座。

\subsubsection{特征对齐模块}{}

特征对齐模块旨在通过显式约束打破异构模态间的分布壁垒，使模型能够跨越物理属性的差异，学习到更具泛化性的通用表征。在 Embedding 模块分别提取出 IMU 动力学特征 $Z_{IMU}$ 与 WiFi 射频特征 $Z_{CSI}$ 后，由于两者在采集机理与量纲分布上的本质不同，直接融合往往难以诱发有效的语义共鸣。为此，本研究参考对比语言-图像预训练（CLIP）的思想，在特征融合前引入跨模态对齐机制。

设输入的一个批次数据中，特征向量集合表示为 $Z_I \in \mathbb{R}^{N \times D}$ 与 $Z_W \in \mathbb{R}^{N \times D}$，其中 $N$ 为批次大小，$D$ 为特征维度。为了度量两类模态在同一行为下的相关性，模块首先构建跨模态相似度矩阵 $M$：
\begin{equation}
M = Z_I \cdot Z_W^\top
\end{equation}
该矩阵中的元素 $M_{i,j}$ 表征了第 $i$ 个 IMU 样本与第 $j$ 个 CSI 样本在潜在空间中的匹配程度。

在该相似度矩阵的约束下，对齐模块的训练逻辑遵循对比学习范式：
其一，对于对角线元素 $M_{i,i}$，由于其描述的是同一时刻、同一物理事件的异构观测，模型需最大化其相似度，从而实现特征的对齐性。
其二，对于非对角线元素 $M_{i,j} (i \neq j)$，由于其对应不相关的动作状态，模型需抑制其相似度能量，以保证特征分布的均匀性。

这种基于相似度矩阵的对齐操作，迫使 Mamba 编码器在特征提取阶段自动过滤掉仅存在于单模态数据中的局部噪声（如 WiFi 的多径衰落或 IMU 的硬件温漂），转而聚焦于对人体活动具有判别力的本质特征。这种机制不仅缩减了异构模态间的分布距离，更为后续的多任务级联融合提供了一个语义一致、信息互补的特征底座。

\subsubsection{特征融合模块}{}

特征融合模块的任务是将经过对齐处理的异构特征 $Z_{IMU}$ 与 $Z_{CSI}$ 整合为统一的全局表征。在对比学习机制的约束下，两类特征已在潜在空间内达成了语义一致性，这为后续的特征级联提供了坚实的逻辑基础。为了在融合过程中最大限度地保留各模态的特异性信息，避免因加权求和等操作导致判别性特征被稀释，本研究采用了特征级联（Concatenation）的融合策略。

融合过程通过将两个特征向量在维度上进行物理拼接实现，其形式化表述如下：
\begin{equation}
Z_{fused} = [Z_{IMU} \oplus Z_{CSI}]
\end{equation}
其中，$\oplus$ 表示特征拼接算子，$Z_{fused}$ 即为融合后的联合特征向量。

通过这种方式，来自 IMU 的局部动作细节特征与来自 WiFi 的宏观环境扰动特征被完整地耦合在一起。这种多维度的特征堆叠不仅增强了系统对复杂活动的描述能力，也确保了后续多任务解码模块能够基于全量的观测信息进行精确推理。

\subsubsection{分类模块}{}

分类模块作为 WICTIVITY 架构的推理末端，旨在利用融合后的全局表征 $Z_{fused}$ 协同完成多维度的感知任务。由于 $Z_{fused}$ 已经集成了来自 IMU 的动力学纹理与来自 WiFi 的空间电磁扰动特征，且经过对比学习的语义对齐，其内部蕴含了足以支撑多种下游判别任务的判别性信息。本模块通过设计三个并行的任务专用解码头（Task-specific Heads），分别实现人体活动识别（HAR）、用户身份识别（User ID）以及实验环境分类（Environment Classification）。

在结构实现上，各分支均采用多层感知机（Multi-Layer Perceptron, MLP）构建，通过非线性映射将高维的联合特征映射至对应的标签概率空间。以活动识别分支为例，预测过程可表述为：
\begin{equation}
P_{act} = \sigma (\text{MLP}_{act}(Z_{fused}))
\end{equation}
其中，$\text{MLP}_{act}$ 表示活动识别专用映射层，$\sigma$ 为 Softmax 激活函数，$P_{act}$ 为模型输出的活动类别概率分布。

同理，身份识别分支与环境感知分支分别对融合特征进行独立的非线性解码，以获取关于用户生物特征与场景多径特征的预测结果：
\begin{equation}
P_{id} = \sigma (\text{MLP}_{id}(Z_{fused}))
\end{equation}
\begin{equation}
P_{env} = \sigma (\text{MLP}_{env}(Z_{fused}))
\end{equation}
这种多分支并行推理的设计模式，使得模型能够在共享底层时序特征提取能力的同时，兼顾不同任务对特征侧重点的差异。通过该模块的并行解码，系统实现了从单一多模态信号输入到多维度状态感知的端到端映射，为后续的总损失函数构建与多任务联合训练奠定了输出基础。

\subsubsection{损失函数}

为了实现 WICTIVITY 架构在多任务学习范式下的联合优化，本研究设计了一个多目标加权损失函数。该损失函数的核心逻辑在于：利用跨模态对比学习损失约束特征底座的对齐性，同时利用多个并行分类损失确保模型对人体活动、用户身份及实验环境的判别能力。通过对全局损失函数的最小化，模型能够在异构信号映射过程中挖掘到跨模态不变的通用特征表达。

本模型的总损失函数 $\mathcal{L}_{total}$ 由四个分量加权组成，其完整数学表达式如下：
\begin{equation}
\mathcal{L}_{total} = \alpha \mathcal{L}_{act} + \beta \mathcal{L}_{id} + \gamma \mathcal{L}_{env} + \zeta \mathcal{L}_{cl}
\end{equation}
其中，$\alpha, \beta, \gamma$ 和 $\zeta$ 分别为调节活动识别、身份鉴定、环境感知以及对比对齐任务重要程度的加权系数。

在公式中，跨模态对比损失 $\mathcal{L}_{cl}$ 作用于特征对齐模块生成的相似度矩阵 $M$。为了实现 IMU 与 WiFi 特征在潜在空间中的语义统一，我们采用对称交叉熵准则对相似度矩阵的行（IMU 维度）与列（CSI 维度）同时进行约束，其定义为：
\begin{equation}
\mathcal{L}_{cl} = \frac{1}{2} \left[ -\frac{1}{N} \sum_{i=1}^{N} \log \frac{\exp(M_{i,i}/\tau)}{\sum_{j=1}^{N} \exp(M_{i,j}/\tau)} - \frac{1}{N} \sum_{i=1}^{N} \log \frac{\exp(M_{i,i}/\tau)}{\sum_{j=1}^{N} \exp(M_{j,i}/\tau)} \right]
\end{equation}
其中 $N$ 为批次大小，$\tau$ 为可学习的温度参数。该损失项强制模型最大化对角线上的正样本对得分，并抑制非对角线的噪声干扰。

下游分类任务的损失项（$\mathcal{L}_{act}, \mathcal{L}_{id}, \mathcal{L}_{env}$）则统一遵循多分类交叉熵准则。以活动识别损失为例，通过对比预测概率分布 $P_{act}$ 与真实标签向量 $Y_{act}$ 的散度，驱动模型从级联特征 $Z_{fused}$ 中提取特定的运动动力学信息：
\begin{equation}
\mathcal{L}_{act} = -\frac{1}{N} \sum_{n=1}^{N} \sum_{k=1}^{K} Y_{act, n, k} \log(P_{act, n, k})
\end{equation}
其中 $K$ 代表活动类别的总数。同理，身份识别与环境分类损失亦采用上述形式构建。

通过这种联合优化策略，WICTIVITY 模型不仅能够在训练过程中实现模态间的表征共鸣，还能够通过多任务的相互正则化作用，提升模型在复杂场景下的鲁棒性与泛化精度。

\xsubsection{跨域少样本适配策略}{}

人体活动识别在实际应用中面临的首要挑战是不同受试者间的生理特异性。受试者的肢体长度、运动习惯以及传感器佩戴位置的微小差异，会导致同一动作类别的信号在特征空间中呈现出显著的非一致性，即领域偏移。预训练模型在源域（预训练受试者）上习得的分类决策边界，往往难以直接适应目标域（未见受试者）产生的分布偏差。这种由于个体差异带来的泛化瓶颈，会导致模型在跨受试者测试时识别精度大幅下降。为了解决这一问题，本文提出一种基于全局语义锚点对齐的少样本适配策略，通过轻量化的模型微调与特征对齐机制，实现从“通用模型”向“个性化模型”的快速迁移。

该策略的核心思路是利用源域大规模数据建立一套稳定的类别特征中心作为全局锚点。在面对新受试者时，仅需获取极少量带有标签的支撑样本，通过修正特征分布使目标域特征向对应的全局锚点靠拢，从而校准由于个体差异引发的语义偏移。这种方案能够在保障特征表示一致性的前提下，显著降低针对新用户进行模型部署的标定成本。

在模型架构修改层面，本文在 WINet 活动识别分支的融合特征层后嵌入了瓶颈适配器（Bottleneck Adapter）。该模块通过残差连接结构接入原网络，其核心由下投影层 $W_{down} \in \mathbb{R}^{d \times (d/r)}$、非线性激活函数以及上投影层 $W_{up} \in \mathbb{R}^{(d/r) \times d}$ 构成，其中 $r$ 为缩减因子。其前向计算过程定义如下：
\begin{equation}
x_{out} = x + \text{ReLU}(x \cdot W_{down}) \cdot W_{up}
\end{equation}
在适配阶段，本文采用参数高效微调（PEFT）方案，通过冻结主干网络权重，仅将适配器参数设为可学习状态。为了保证微调初期的数值稳定性，上投影层 $W_{up}$ 的权重被初始化为零矩阵。这种设计确保了在适配起始时刻，模型输出与预训练模型保持一致，随后通过梯度下降逐步学习针对目标受试者的特异性补偿特征，实现了对原有特征空间的非线性校准。

在对齐机制层面，本文设计了原型对齐损失（Prototypical Alignment Loss, PAL）用以拉近目标域与源域间的特征距离。首先，在适配开始前，利用源域全量训练样本 $D_s$ 计算每个动作类别 $k$ 的全局语义锚点 $p_k^s$。设 $f(\cdot)$ 为经过 $L_2$ 归一化处理的特征提取映射，则全局锚点的计算公式如下：
\begin{equation}
p_k^s = \frac{1}{|D_s^k|} \sum_{x_i \in D_s^k} f(x_i)
\end{equation}
在针对目标受试者的适配过程中，利用其包含的 $n$ 个带有标签的支撑样本，计算目标域在该时刻下的局部特征质心 $\mu_k^t$。为了强制目标域特征分布向源域的标准中心对齐，PAL 损失通过度量两者的余弦相似度来构建几何约束，其数学表达式为：
\begin{equation}
L_{pal} = \frac{1}{K} \sum_{k=1}^{K} \left( 1 - \frac{\mu_k^t \cdot p_k^s}{\|\mu_k^t\|_2 \|p_k^s\|_2} \right)
\end{equation}
结合活动识别的主任务损失 $L_{task}$ 与跨模态对比学习损失 $L_{clip}$，系统的总优化目标函数 $L_{total}$ 构建如下：
\begin{equation}
L_{total} = \alpha L_{task} + \beta L_{clip} + \gamma L_{pal}
\end{equation}
其中，$\alpha, \beta, \gamma$ 为调节各项任务权重的超参数。该损失函数作为一种强有力的归纳偏置，强迫适配器学习一种跨域一致的特征映射，使偏离的特征点能够以全局语义锚点为基准进行平移与聚拢。

综上所述，本文提出的跨域少样本适配策略通过在架构中引入残差适配器，并在优化目标中嵌入基于原型对齐的几何约束，构建了一套完备的个体差异校准流程。这种设计不仅保障了模型在面对新用户时的快速响应能力，更通过全局锚点的引导确保了特征空间在跨受试者场景下的分布稳定性，为实现鲁棒的人体感知奠定了算法基础。

\xsection{实验与结果分析}{References}

\xsubsection{评价指标}{Ref equations}

为了全面、客观且定量地评估本文提出的基于多模态融合的多任务人体活动识别模型的性能，本实验构建了包含全局指标与细粒度指标的综合评价体系。其中，采用准确率（Accuracy）作为衡量模型整体分类性能的核心指标，用于直观反映模型在测试集上的总体表现；同时，引入混淆矩阵（Confusion Matrix）作为误差分析工具，旨在揭示模型在不同类别间的误判分布规律，从而深入剖析多模态特征在不同任务中的有效性与局限性。

\paragraph{准确率}{}

准确率是分类任务中最为直观且通用的评价指标，其定义为模型在测试数据集中正确分类的样本数占总样本数的比例。该指标能够从宏观角度反映模型对数据分布的学习能力和泛化性能。考虑到本文提出的模型是一个多任务协同学习框架，旨在利用同一套特征提取网络同时完成活动识别、身份识别和场景识别三个具有内在关联的任务，因此，本实验针对这三个子任务分别计算其准确率，以综合衡量模型在不同语义维度上的感知能力。

假设测试集中共有 $N$ 个样本，对于第 $i$ 个样本，其真实的活动标签、身份标签和场景标签分别记为 $y_{i}^{act}$、$y_{i}^{id}$ 和 $y_{i}^{scene}$，而模型输出的对应预测标签分别记为 $\hat{y}_{i}^{act}$、$\hat{y}_{i}^{id}$ 和 $\hat{y}_{i}^{scene}$。则各任务的准确率计算公式如下所示：

\begin{equation}
\text{Accuracy}_{task} = \frac{1}{N} \sum_{i=1}^{N} \mathbb{I}(\hat{y}_{i}^{task} = y_{i}^{task}), \quad task \in \{act, id, scene\}
\end{equation}

其中，$\mathbb{I}(\cdot)$ 为示性函数（Indicator Function），当且仅当括号内的条件成立（即预测标签与真实标签完全一致）时，函数取值为1，否则取值为0。基于该公式，我们可以得到三个独立的评价指标：$\text{Accuracy}_{act}$ 代表活动识别准确率，反映了模型对人体运动学特征的捕捉能力；$\text{Accuracy}_{id}$ 代表身份识别准确率，反映了模型对用户体型、步态等生物特征的区分能力；$\text{Accuracy}_{scene}$ 代表场景识别准确率，反映了模型利用Wi-Fi信号感知环境多径特征的能力。这三个指标共同构成了评价多模态多任务模型性能的基石。

\paragraph{混淆矩阵}{}

虽然准确率能够有效地量化模型的全局性能，但在多分类问题中，单一的数值指标往往掩盖了类别间的分布差异，无法揭示模型在具体类别上的误判细节。例如，模型可能在某些简单动作上表现优异，而在某些相似动作上存在严重的混淆。为了进行更细粒度的误差诊断，本文引入了混淆矩阵进行可视化分析。

混淆矩阵 $M$ 是一个 $C \times C$ 的二维矩阵，其中 $C$ 表示特定分类任务（如活动分类）的类别总数。矩阵中的每一行代表样本的真实归属类别，每一列代表模型预测的类别。矩阵中第 $i$ 行第 $j$ 列的元素 $M_{ij}$ 的数值表示真实标签为类别 $i$ 但被模型预测为类别 $j$ 的样本数量。基于混淆矩阵的分析主要关注以下两个方面：

首先是对角线元素分析。矩阵的主对角线元素（即 $i=j$ 处的值）表示模型将某类样本正确预测为该类的数量。在可视化热力图中，对角线上的颜色越深或数值越大，说明模型在该类别上的查全率（Recall）越高，识别效果越好。通过观察对角线元素的分布，可以判断模型是否存在对某些特定类别的识别短板。

其次是非对角线元素分析。矩阵中的非对角线元素（即 $i \neq j$ 处的值）表示模型产生的误判（Confusion）。通过观察非对角线上的高值区域，可以直观地发现哪些类别之间容易产生混淆。例如，在活动识别任务中，如果“走”和“跑”对应的交叉位置数值较高，说明模型难以区分这两种相似的运动模式；在场景识别任务中，如果“卧室”和“书房”存在混淆，可能暗示这两个场景的Wi-Fi多径特征具有较高的相似性。

通过结合准确率的宏观评估与混淆矩阵的微观分析，本实验不仅能验证模型在XRFV2数据集上的有效性，还能深入探究多模态特征在处理复杂语义冲突时的鲁棒性，为后续的模型优化提供明确的数据支撑。

\xsubsection{实验环境}{Level of this document}

\begin{figure}[h]
  \centering
  \includegraphics[width=\textwidth]{confusion_matrix.png}
  \caption{transformer}
\end{figure}

\xsubsection{数据实验结果分析}{}

\subsubsection{模态消融实验}{}

为了全面评估WICTIVITY模型中各输入模态对人体活动识别、身份识别及场景识别这三个联合任务的贡献度，并验证多模态融合策略的必要性与有效性，本节开展了系统的模态消融实验。实验通过有选择地激活或屏蔽输入端的数据通道，对比了单模态输入（如仅IMU、仅WiFi CSI原始数据及其不同分量、仅WiFi CSI的时频图STFT数据及其分量）与多模态组合输入下的模型性能。表3-X详细展示了不同输入配置下模型在测试集上的三个任务的准确率表现。其中，“wifi”指代CSI的原始时域幅度和相位数据，“stft”指代经过短时傅里叶变换后的时频图谱数据。

\begin{table}[H]
  \centering
  \caption{模态消融实验结果}
  \begin{tblr}{
    colspec={X[c] X[c] X[c] X[c]},
  }
  \toprule
  模态 & 活动准确率 & 身份准确率 & 场景准确率 \\
  \midrule

  imu            & 92.96\% & 96.79\% & 97.83\% \\
  wifi           & 87.96\% & 99.70\% & 99.92\% \\
  wifi\_amp      & 87.75\% & 99.52\% & 99.89\% \\
  wifi\_pha      & 84.85\% & 97.86\% & 100.00\% \\
  stft           & 64.34\% & 96.63\% & 100.00\% \\
  stft\_amp      & 68.45\% & 97.54\% & 99.99\% \\
  stft\_phase    & 23.94\% & 63.80\% & 97.56\% \\
  imu + wifi     & 96.06\% & 99.93\% & 99.99\% \\
  imu + wifi\_amp & 95.68\% & 99.93\% & 99.95\% \\
  imu + wifi\_pha & 94.65\% & 99.70\% & 99.99\% \\
  imu + stft     & 94.00\% & 99.66\% & 99.92\% \\
  imu + stft\_amp & 93.39\% & 99.63\% & 99.88\% \\
  imu + stft\_pha & 90.24\% & 96.58\% & 98.78\% \\

  \bottomrule
  \end{tblr}
\end{table}

深入分析表3-X中的实验数据，我们可以从单模态特性与多模态互补性两个维度揭示数据背后的物理规律。首先，就单模态表现而言，惯性传感器（IMU）与无线信号（WiFi）在不同任务上展现出了显著的性能偏好。IMU数据在活动识别任务上取得了92.96\%的较高基线准确率，优于原始WiFi数据的87.96\%。这符合物理直觉，因为IMU直接佩戴于肢体末端，能够精准捕捉人体运动的加速度与角速度变化，对快速、细粒度的动作捕捉更为敏感。相比之下，WiFi CSI信号虽然也能反映人体运动引起的信道变化，但其更容易受到环境噪声和非视距传播路径的影响，在精细动作区分度上略逊一筹。然而，在身份识别与场景识别任务上，WiFi模态展现出了压倒性的优势。原始WiFi输入的身份与场景准确率分别高达99.70\%和99.92\%，而IMU在这两个任务上的表现分别为96.79\%和97.83\%。这是因为WiFi信号在特定空间环境中的多径传播特性构成了独特的“环境指纹”，使得场景识别极为准确；同时，不同个体的体型、身高和体脂分布对射频信号的散射与吸收模式存在差异，形成了稳定的“生物射频特征”，从而赋予了WiFi信号极高的身份辨识能力。

其次，针对WiFi信号的不同处理方式进行深入对比。在时域信号中，幅度信息（wifi\_amp）在活动识别上的贡献（87.75\%）显著优于相位信息（wifi\_pha, 84.85\%），这表明在未经复杂校准的情况下，CSI振幅对人体宏观运动的响应更为稳定，而原始相位信息往往包含较多因硬件不同步导致的随机噪声。一个值得注意的现象是，基于STFT变换的频域输入在活动识别任务上的表现普遍低于对应的时域原始信号输入。例如，完整的STFT输入仅获得64.34\%的活动准确率，远低于原始WiFi输入的87.96\%；仅使用STFT相位数据时，活动准确率更是跌至23.94\%。这一结果揭示了在处理短时人体动作时，时域信息的完整性至关重要。STFT变换虽然提供了频域视角，但窗口操作不可避免地平滑了信号的瞬态时序特征，导致关键的动作起始与转换信息丢失。特别是对于相位数据，频域变换可能进一步放大了噪声的影响，导致特征几乎不可用。

最后，多模态融合实验结果有力地证明了所提方法的有效性。当结合IMU与原始WiFi数据（imu + wifi）时，模型在所有三个任务上均达到了最佳性能，活动识别准确率提升至96.06\%，相较于最优单模态（IMU）提升了3.1个百分点，同时身份和场景识别准确率接近饱和（分别为99.93\%和99.99\%）。这种显著的性能提升验证了IMU与WiFi模态之间存在高度的互补性：IMU提供了精确的肢体动力学信息，弥补了WiFi信号对细节动作不敏感的缺陷；而WiFi信号则提供了丰富的环境上下文与人体生物特征全局信息，增强了模型在复杂场景下的鲁棒性。此外，对比“imu + wifi”（活动96.06\%）与“imu + stft”（活动94.00\%）的结果可知，在融合系统中，保留WiFi信号的原始时序结构比将其转换为时频图谱更为有利，这也再次印证了时域特征在当前数据集动作识别任务中的核心地位。综上所述，采用IMU与原始WiFi时域信号进行特征级融合，是实现高精度、多任务联合识别的最佳策略。

\subsubsection{设备消融实验}{}

为了进一步探究多模态感知系统中单个硬件设备对整体性能的贡献度，并评估系统在部分传感器失效情况下的鲁棒性，本节开展了详细的设备消融实验。该实验采用“留一法”策略，即在测试阶段每次人为移除或屏蔽一个特定设备（IMU节点或WiFi链路）的数据输入，而保留其他所有设备的正常输入，以此观察模型在人体活动识别、身份识别和场景识别三个联合任务上的性能变化。表3-X展示了基准状态（所有设备正常工作）与分别移除不同编号的IMU和WiFi设备后的准确率对比结果。

\begin{table}[H]
  \centering
  \caption{设备消融实验结果}
  \begin{tblr}{
    colspec={X[c] X[c] X[c] X[c] X[c]},
  }
  \toprule
  设备 & 设备编号 & 活动准确率 & 身份准确率 & 场景准确率 \\
  \midrule

  -    & -    & 96.06\% & 99.93\% & 99.99\% \\
  imu  & 0    & 94.86\% & 99.85\% & 99.90\% \\
  imu  & 1    & 95.67\% & 99.94\% & 99.96\% \\
  imu  & 2    & 94.79\% & 99.82\% & 99.96\% \\
  imu  & 3    & 95.34\% & 99.86\% & 99.95\% \\
  imu  & 4    & 95.65\% & 99.88\% & 99.96\% \\
  wifi & 0    & 95.53\% & 99.76\% & 99.95\% \\
  wifi & 1    & 94.80\% & 99.77\% & 99.95\% \\
  wifi & 2    & 94.79\% & 99.71\% & 99.94\% \\

  \bottomrule
  \end{tblr}
\end{table}

分析表3-X中的实验数据，首先可以观察到，相较于全设备工作的基准状态（活动准确率96.06\%，身份准确率99.93\%，场景准确率99.99\%），移除任何单一设备都会导致模型在所有任务上的性能出现不同程度的下降。这一普遍性的下降趋势有力地证明了本系统所部署的每一个传感器节点都在提供具有互补性的有效信息，多视角的数据融合对于达成最优性能至关重要。同时，实验结果也显示出系统具备一定的鲁棒性，即使缺失单一设备，模型依然能够维持较高的识别精度，并未出现灾难性的性能崩溃。

深入对比不同类型设备的消融结果，可以揭示出不同传感器在任务中的具体作用差异。对于活动识别任务，不同位置的IMU节点表现出了显著的重要性差异。其中，移除编号为2的IMU设备导致活动准确率下降幅度最大，跌至94.79\%；移除编号为0的IMU设备次之，准确率为94.86\%。在典型的惯性动作捕捉配置中，这些关键节点往往佩戴于人体躯干核心（如腰部或骨盆）或主导肢体，它们对于捕捉人体整体位移、质心变化以及核心动作模式具有决定性作用，其数据的缺失难以被边缘节点的信号完全补偿。相比之下，移除IMU 1或IMU 4对性能影响较小，准确率仍保持在95.6\%以上，这表明这些节点所提供的信息与其他节点存在较高的冗余度，或者它们所监测的身体部位在当前数据集包含的特定活动类别中活跃度较低。

在WiFi设备消融方面，移除单一WiFi链路对活动识别的影响与关键IMU节点缺失的影响在同一量级，准确率在94.79\%至95.53\%之间波动，这再次印证了CSI信号对人体运动特征的有效捕捉能力。值得注意的是，相较于IMU的移除，WiFi链路的缺失对身份识别任务造成了更为明显的负面影响，身份准确率最低降至99.71\%。这一现象进一步支撑了前述模态消融实验的结论，即WiFi信号中蕴含的受试者特有的生物射频特征对身份鉴别起着关键作用，任何一条射频传播路径的缺失都会削弱这种“环境指纹”的完整性。对于场景识别任务，由于其本身极高的基线准确率以及WiFi信号对环境特征的强全局感知能力，单一设备的移除几乎没有造成显著影响，准确率始终维持在99.90\%以上的高位。综上所述，设备消融实验验证了多设备融合的必要性，并量化了不同物理位置传感器节点的贡献差异，为后续的传感器网络优化部署提供了重要依据。

\subsubsection{数据分割实验}{}

在处理连续的多模态时间序列传感器数据时，滑动窗口技术是将原始数据流切片为适合模型输入的离散样本的核心预处理步骤。窗口大小（window size）决定了单个样本所包含的时间上下文跨度，而步长（stride）则控制了相邻窗口之间的重叠程度以及样本生成的密度。本实验旨在系统地探究不同数据分割策略对模型在人体活动识别、身份识别及场景识别这三个联合任务上性能的影响，从而确定能够平衡信息充分性与时间分辨率的最佳参数配置。表3-X详细展示了窗口大小从25变化至150、步长从25变化至150的多种组合下的实验结果。

\begin{table}[H]
  \centering
  \caption{数据分割实验结果}
  \begin{tblr}{
    colspec={X[2,c] X[1,c] X[1,c] X[1,c]},
  }
  \toprule
  数据分割方案 & 活动准确率 & 身份准确率 & 场景准确率 \\
  \midrule

  window\_size = \ 25, stride = \ 25  & 88.83\% & 99.73\% & 99.95\% \\
  window\_size = \ 50, stride = \ 25  & 96.06\% & 99.93\% & 99.99\% \\
  window\_size = \ 75, stride = \ 25  & 98.50\% & 99.96\% & 99.99\% \\
  window\_size = 100, stride = \ 25  & 99.27\% & 99.96\% & 99.99\% \\
  window\_size = 125, stride = \ 25  & 99.62\% & 99.99\% & 99.99\% \\
  window\_size = 150, stride = \ 25  & 99.79\% & 99.98\% & 99.96\% \\
  window\_size = \ 50, stride = \ 50  & 88.71\% & 99.52\% & 99.90\% \\
  window\_size = \ 75, stride = \ 50  & 94.47\% & 99.85\% & 99.94\% \\
  window\_size = 100, stride = \ 50  & 96.95\% & 99.94\% & 99.93\% \\
  window\_size = 125, stride = \ 50  & 97.50\% & 99.68\% & 99.73\% \\
  window\_size = 150, stride = \ 50  & 98.77\% & 99.93\% & 99.90\% \\
  window\_size = \ 75, stride = \ 75  & 89.85\% & 99.54\% & 99.71\% \\
  window\_size = 100, stride = \ 75  & 91.82\% & 99.21\% & 99.85\% \\
  window\_size = 125, stride = \ 75  & 92.73\% & 98.38\% & 99.44\% \\
  window\_size = 150, stride = \ 75  & 96.33\% & 99.39\% & 99.72\% \\
  window\_size = 100, stride = 100 & 91.31\% & 99.12\% & 99.71\% \\
  window\_size = 125, stride = 100 & 92.34\% & 98.48\% & 99.30\% \\
  window\_size = 150, stride = 100 & 93.69\% & 98.40\% & 99.27\% \\
  window\_size = 125, stride = 125 & 90.87\% & 96.81\% & 99.00\% \\
  window\_size = 150, stride = 125 & 94.91\% & 99.28\% & 99.70\% \\
  window\_size = 150, stride = 150 & 93.21\% & 98.52\% & 99.30\% \\

  \bottomrule
  \end{tblr}
\end{table}

深入分析表3-X中的数据，首先可以观察到窗口大小对活动识别任务的准确率有着决定性的影响。在固定较小步长（例如 stride = 25）的前提下，随着窗口大小从25逐步增加到150，活动识别准确率呈现出显著且持续的上升趋势，从88.83\%飞跃至接近饱和的99.79\%。这一现象深刻揭示了人体活动的内在时序特性：许多复杂的动作具有一定的持续时间和演变过程，过短的时间窗口往往只能捕捉到动作的瞬态片段或过渡阶段，导致特征具有高度的歧义性；而较大的窗口能够涵盖更完整的动作周期或包含足够的上下文信息，使得模型能够提取到更具判别力的长时序依赖特征。其次，步长参数，即数据重叠率的倒数，也对性能产生关键影响。对比固定窗口大小（例如 window size = 100）的情况，随着步长从25增加到100（意味着重叠率逐渐降低直至零重叠），活动准确率从99.27\%逐渐下降至91.31\%。较小的步长意味着较高的窗口重叠率，这实际上起到了一种数据增强的作用，确保了动作转换边界被密集采样，从而提高了模型对边界条件的鲁棒性；反之，过大的步长虽然减少了计算上的冗余，但也可能导致关键的过渡信息丢失。

与活动识别任务形成鲜明对比的是，身份识别和场景识别任务对分割参数的变化表现出了极高的鲁棒性。在绝大多数配置下，这两项任务的准确率均维持在99\%以上的高位，场景识别准确率甚至在多个配置下达到99.99\%。这表明，决定用户身份的生物特征（如体型、步态引发的信号散射模式）和决定场景类型的环境特征（如空间多径指纹）在时间上是相对稳定的，它们不像动态活动那样依赖于长时序上下文信息。尽管如此，当步长非常大导致重叠率极低时（例如 window size = 125, stride = 125），身份识别准确率仍出现了轻微的下滑（降至96.81\%），这暗示保持一定程度的时间连续性对于维持最稳定的身份推断仍然是有益的。综合考量三个任务的性能表现以及计算效率，较大的窗口大小配合较小的步长（例如 window size = 100, stride = 25）能够提供最佳的整体性能，确保了在捕捉足够时序信息的同时，通过高重叠采样最大化了数据的利用率。

\xsubsection{网络架构实验结果分析}{Ref equations}

\subsubsection{骨干网络选择实验}{}

为了构建高效且鲁棒的多模态人体活动识别系统，选择具备强大时序特征提取能力的核心骨干网络至关重要。骨干网络作为深度学习模型的基础特征编码器，其能否从高维、非线性的多模态传感器数据流中捕获长期依赖关系和复杂的时空动态模式，直接决定了后续分类任务的性能天花板。因此，本节开展了网络架构对比实验，旨在通过定量评估不同主流神经网络架构作为特征提取器时的表现，甄选出最适配当前多模态时序数据特性的骨干网络模型。本实验选取了五种在时间序列分析领域具有代表性的网络架构进行横向对比研究。其中包括经典的卷积神经网络变体ResNet18（针对一维信号适配），擅长处理序列数据的循环神经网络变体LSTM（长短期记忆网络）和GRU（门控循环单元），基于全局自注意力机制的Transformer架构，以及近年来在长序列建模中崭露头角的状态空间模型Mamba。在保持其他实验设置（如输入数据预处理、融合策略和分类头结构）完全一致的前提下，仅替换特征提取网络的结构，记录并对比了各模型在活动识别、身份识别和场景识别三个任务上的准确率，具体结果如表3-X所示。

\begin{table}[H]
  \centering
  \caption{骨干网络选择实验结果}
  \begin{tblr}{
    colspec={X[c] X[c] X[c] X[c]},
  }
  \toprule
  骨干网络 & 活动准确率 & 身份准确率 & 场景准确率 \\
  \midrule

  resnet18    & 88.44\% & 98.75\% & 99.99\% \\
  mamba       & 96.06\% & 99.93\% & 99.99\% \\
  lstm        & 91.43\% & 99.68\% & 99.91\% \\
  gru         & 92.90\% & 99.90\% & 99.99\% \\
  transformer & 95.06\% & 99.95\% & 99.97\% \\

  \bottomrule
  \end{tblr}
\end{table}

深入剖析表3-X中的实验数据，可以清晰地观察到不同网络架构设计理念对时序数据处理能力的差异，尤其是在最具挑战性的活动识别任务上，各模型的性能分化最为显著。ResNet18作为基准模型，其活动识别准确率最低，仅为88.44\%。这主要是因为基于局部卷积核的架构虽然在提取短时频域特征方面表现尚可，但其受限的感受野使得它难以有效捕捉跨越长时间跨度的复杂动作依赖关系，在处理包含丰富长时序信息的传感器数据流时存在天然局限。相比之下，LSTM和GRU这类循环神经网络凭借其内部的门控机制和状态记忆单元，天然适合处理序列数据，因此在活动识别上取得了优于ResNet的表现，准确率分别提升至91.43\%和92.90\%。然而，RNN结构的串行计算特性限制了其优化效率，且在面对较长输入序列时仍不可避免地面临梯度衰减或长距离信息遗忘的问题。基于自注意力机制的Transformer模型进一步将活动准确率提升至95.06\%。其核心优势在于能够直接计算序列中任意时间步之间的相关性权重，从而高效捕获全局上下文信息，有效突破了RNN的短时记忆瓶颈。

最为显著的是，本研究引入的状态空间模型Mamba在本实验中展现出了压倒性的优势，其活动识别准确率高达96.06\%，不仅大幅超越了传统的CNN和RNN架构，也优于流行的Transformer模型。Mamba的卓越表现归因于其独特的选择性结构化状态空间序列机制（Selective Structured State Space Sequence Models）。与Transformer的二次方计算复杂度不同，Mamba具备处理长序列的线性计算复杂度，这使其能够在不增加过多计算负担的前提下，更有效地整合极长范围内的历史信息。此外，其基于连续时间系统的建模本质更加契合物理世界传感器信号的连续动态特性，从而能够提取到更具判别力和鲁棒性的时序特征表征。在身份识别和场景识别任务上，尽管由于任务本身的高基线使得各模型的差异不如活动识别那样显著，但Mamba依然保持了最优的性能（身份准确率99.93\%，场景准确率已达饱和的99.99\%），进一步验证了其作为通用时序特征提取器的强大泛化能力。综上所述，鉴于Mamba模型在处理复杂多模态时间序列数据方面展现出的最佳综合性能和长序列建模优势，本研究最终确定选用Mamba作为后续多模态融合框架的核心骨干网络。

\subsubsection{模态融合方案选择实验}{}

在确定了采用Mamba作为高效的时序特征提取基座后，下一个关键环节在于如何有效地融合来自惯性测量单元（IMU）和无线信道状态信息（WiFi CSI）这两个异构模态的高维特征表征。不同的模态融合策略决定了信息交互的方式与深度，直接影响多模态互补优势的发挥。为了甄选出最适配当前数据特性与多任务需求的融合机制，本节开展了模态融合方案对比实验。实验主要对比了两种在深度学习领域广泛应用的特征级融合策略：基础高效的特征拼接（Concatenation）与建模能力更强的交叉注意力（Cross-Attention）。

\begin{table}[H]
  \centering
  \caption{模态融合方案选择实验结果}
  \begin{tblr}{
    colspec={X[c] X[c] X[c] X[c]},
  }
  \toprule
  模态融合方案 & 活动准确率 & 身份准确率 & 场景准确率 \\
  \midrule

  拼接       & 96.20\% & 99.84\% & 99.99\% \\
  交叉注意力  & 96.06\% & 99.93\% & 99.99\% \\

  \bottomrule
  \end{tblr}
\end{table}

特征拼接是一种直接线性的融合方式，它将不同模态骨干网络输出的特征向量在通道维度上进行级联，构建一个包含所有模态信息的联合特征长向量，随后将其送入下游的多任务分类网络进行处理。这种方式的潜在假设是后续的全连接层等结构能够通过学习自动挖掘模态间的非线性组合关系。相比之下，交叉注意力机制则更为复杂精细，它通过将一个主导模态的特征映射为查询项（Query），将另一个辅助模态的特征映射为键项（Key）和值项（Value），利用点积注意力机制动态地计算模态间的相关性权重矩阵。这种机制旨在实现不同模态特征在时序或语义空间上的软对齐与加权融合，显式地建模异构数据间的交互依赖关系。

表3-X详细展示了这两种截然不同的融合方案在人体活动识别、身份识别和场景识别三个联合任务上的最终性能表现。观察实验数据可以得出结论，在强大的骨干网络支撑下，两种融合策略均能驱动模型达成极为优异的识别精度，这表明经过前置编码器提取的单模态特征已经具备了较高的质量与判别力。从具体数值来看，特征拼接方案在作为首要任务的活动识别上取得了最高的准确率，达到96.20\%，以微弱的优势领先于交叉注意力方案的96.06\%。而在对细粒度特征更为敏感的身份识别任务上，交叉注意力方案则表现出轻微的优势，准确率为99.93\%，略高于拼接方案的99.84\%。对于场景识别任务，由于任务本身的特征显著性极高，两种方案均达到了近乎饱和的99.99\%准确率，差异可以忽略不计。

深入剖析上述性能差异背后的内在机理，可以从特征的互补性与任务特性角度进行解读。对于活动识别任务而言，其核心挑战在于精准捕捉人体动作的时空动态演变。在本研究中，IMU信号提供了精确的肢体运动学信息，而WiFi信号提供了宏观的身体姿态与环境交互信息，两者在特征层面已具备高度的互补性与独立性。在这种情况下，简单的特征拼接策略能够最大程度地保留各模态特征的原始完整性，给予后续分类网络最大的自由度去学习最优的特征组合权重。反观交叉注意力机制，虽然其设计初衷是增强模态交互，但在特征本身已经高度抽象且互补的情况下，强制的注意力引导可能会引入不必要的归纳偏置，有时反而不如直接拼接来得直接有效。然而，对于身份识别任务，其依赖的是更为稳定且微妙的个体生物特征与行为习惯的耦合（例如特定的步态频率与相应的射频散射模式的对应关系）。交叉注意力机制通过动态对齐不同模态特征，可能更有助于挖掘出那些在时序上微弱但对身份具有独特标识作用的跨模态细粒度关联，因此在身份识别上表现稍好。综合考量三个任务的整体性能平衡，尤其是考虑到特征拼接方案在实现主要目标（活动识别）上的最优表现，以及其相较于注意力机制在计算复杂度和参数量上的显著效率优势，本研究最终决定采用特征拼接作为后续多模态特征融合的标准方案，以在保证高性能的同时维持系统的简洁与高效。

\xsubsection{消融实验结果分析}{Ref equations}

\subsubsection{标准化消融实验}{}

在深度学习模型的训练过程中，数据标准化通常被视为一项关键的预处理步骤，旨在消除不同特征维度间的量纲差异，规范数据分布，从而加速优化算法的收敛并提升模型的泛化能力。特别是对于本研究涉及的惯性测量单元（IMU）和无线信道状态信息（WiFi CSI）这两种异构模态，它们的物理含义、数值范围以及噪声特性存在显著差异。为了系统地评估标准化操作对各模态数据特征学习的影响，并确定最优的预处理策略，本节开展了标准化消融实验。实验采用了标准的Z-Score标准化方法（零均值、单位方差），分别控制是否对输入模型的原始IMU数据和WiFi CSI数据进行标准化处理，形成四种不同的组合配置，并对比它们在人体活动识别、身份识别和场景识别三个联合任务上的性能表现。

\begin{table}[H]
  \centering
  \caption{标准化消融实验结果}
  \begin{tblr}{
    colspec={X[c] X[c] X[c] X[c]},
  }
  \toprule
  IMU标准化 & WiFi标准化 & 活动准确率 & 身份准确率 & 场景准确率 \\
  \midrule

  否 & 否 & 96.06\% & 99.93\% & 99.99\% \\
  是 & 否 & 96.36\% & 99.94\% & 99.99\% \\
  否 & 是 & 95.60\% & 99.91\% & 99.99\% \\
  是 & 是 & 96.30\% & 99.94\% & 99.99\% \\

  \bottomrule
  \end{tblr}
\end{table}

表3-X详细列出了不同标准化策略下的实验结果。观察数据可以发现，虽然不同配置下模型在身份识别和场景识别任务上的准确率均维持在极高的水平（分别在99.9\%和99.99\%附近波动），表明这两个任务对数据分布的细微变化具有较高的鲁棒性，但在最具挑战性的活动识别任务上，标准化策略的选择对性能产生了可观测的影响。具体而言，当不对任何模态进行标准化时（基准线），模型取得了96.06\%的活动识别准确率。在此基础上，仅对IMU数据进行标准化将准确率提升至最高的96.36\%，表明规范化惯性信号分布有助于模型更好地捕捉运动特征。然而，一个值得注意的现象是，仅对WiFi数据进行标准化反而导致活动准确率下降至95.60\%，低于基准线水平。当同时对两者进行标准化时，准确率为96.30\%，介于前两者之间，显示出IMU标准化的正向增益在一定程度上抵消了WiFi标准化的负面影响。

深入剖析上述现象背后的内在机理，可以归因于两种模态信号特性的根本差异。对于IMU数据而言，其包含加速度计和陀螺仪的多个轴向数据，受传感器安装位置、人体运动幅度以及重力分量的影响，不同通道数据的数值范围可能存在较大差异。标准化操作能够有效地将这些数据映射到统一的分布空间，平衡各通道梯度下降的速率，防止数值较大的特征主导损失函数的优化过程，从而提升学习效率和精度。相反，对于WiFi CSI数据，其幅度和相位信息反映的是无线信号在特定环境传播路径中的衰减和延时特性。CSI信号的绝对幅值包含了关于收发距离、环境遮挡以及静态背景的重要信息，而人体运动引起的信号变化往往叠加在这些背景信号之上。对CSI数据进行强制的全局标准化可能会带来两个不利影响：一是可能会压缩包含有效信息的信号动态范围，使得微小的动作特征变得不显著；二是可能会在信号较弱或人体静止时放大环境噪声的相对幅值，降低信噪比，从而干扰模型对真实动作的判断。因此，实验结果表明，对于本研究采用的数据集和模型架构，最佳的预处理策略是对具有显著量纲差异的IMU数据进行标准化，而保留WiFi CSI数据的原始分布特性，以维持其蕴含的丰富环境与动作细节信息。

\subsubsection{对比学习消融实验}{}

在多模态表示学习中，对比学习旨在通过最大化异构模态间的互信息，将不同来源的特征映射到统一的语义空间中，从而消除模态间的分布差异。为了验证本研究引入的基于 CLIP 范式的对比学习机制（Contrastive Learning）对 WICTIVITY 模型性能的影响，本节开展了针对性的消融实验。实验对比了包含对比学习损失项的完整模型（“是”）与仅使用多任务交叉熵损失进行训练的对照模型（“否”），在人体活动识别、身份识别及场景识别三个任务上的准确率，具体结果如表3-X所示。

\begin{table}[H]
  \centering
  \caption{对比学习消融实验结果}
  \begin{tblr}{
    colspec={X[c] X[c] X[c] X[c]},
  }
  \toprule
  消融对比学习 & 活动准确率 & 身份准确率 & 场景准确率 \\
  \midrule

  否 & 96.06\% & 99.93\% & 99.99\% \\
  是 & 94.06\% & 99.75\% & 99.98\% \\

  \bottomrule
  \end{tblr}
\end{table}

观察表3-X中的实验结果可知，引入对比学习机制显著提升了模型在所有识别任务上的表现。在最核心的人体活动识别任务中，准确率从 94.06\% 提升至 96.06\%，实现了 2.0\% 的关键增益；而在基准精度本就较高的身份识别与场景识别任务上，对比学习同样带来了细微但稳健的提升。这一结果有力地证明了显式的特征空间对齐机制能够更有效地挖掘 IMU 动力学特征与 WiFi 射频特征之间的内在关联。

深入剖析该增益背后的内在机理，可以从特征对齐与噪声抑制两个维度进行解释。首先，IMU 采集的是人体局部肢体的加速度与角速度变化，反映的是本体感知信息；而 WiFi CSI 则记录了人体运动对环境电磁场的扰动，反映的是全局空间特征。两者虽然物理属性截然不同，但在描述同一个人体活动时，在高层语义上具有高度的一致性。对比学习通过构建正负样本对，强制模型拉近同一时刻 IMU 特征与 WiFi 特征在超球面上的距离。这种强制对齐不仅缩减了“模态鸿沟”，更促使 Mamba 编码器在特征提取阶段能够剥离单模态私有的随机噪声，转而聚焦于对人体活动具有判别力的本质特征。

其次，对比学习在本质上起到了强正则化的作用。在仅使用分类监督信号的情况下，模型容易过度拟合于某些信噪比较高的模态特征。而引入自监督的对比学习目标后，模型必须在维持模态特异性的同时，学习跨模态的共有模式。这种约束迫使融合后的特征向量具备更强的鲁棒性，从而显著增强了系统在面对复杂运动场景时的识别精度与泛化能力。综上所述，实验结果验证了对比学习在实现高精度多模态人体活动感知中的核心地位，WICTIVITY 模型最终确立了包含对比学习模块的端到端训练框架。

\subsubsection{损失函数权重消融实验}{}

在多任务联合学习（Multi-task Learning）的架构中，总体损失函数通常由各个子任务损失函数的加权和构成。由于人体活动识别、身份识别与场景识别这三个任务在任务难度、收敛速度以及损失值的数量级上存在天然的差异，直接采用等权重的求和方式（即权重均为1）往往难以达到全局最优。简单的等权相加可能导致梯度更新方向被数值较大或收敛较慢的任务主导，或者反之，模型过早地在简单任务上过拟合而忽视了困难任务的优化。为了验证损失函数加权策略在平衡多任务训练中的必要性，本节开展了损失函数权重消融实验。实验对比了采用精心设计的加权策略（“是”）与采用默认等权重策略（“否”）两种配置下模型在三个任务上的最终性能表现。

\begin{table}[H]
  \centering
  \caption{损失函数权重消融实验结果}
  \begin{tblr}{
    colspec={X[c] X[c] X[c] X[c]},
  }
  \toprule
  交叉熵损失函数权重 & 活动准确率 & 身份准确率 & 场景准确率 \\
  \midrule

  是 & 96.06\% & 99.93\% & 99.99\% \\
  否 & 94.76\% & 99.81\% & 99.92\% \\

  \bottomrule
  \end{tblr}
\end{table}

表3-X详细展示了引入损失函数权重调节前后的实验结果。从数据层面进行宏观分析，可以清晰地看到，采用加权策略的实验组在所有三个任务指标上均优于未加权的对照组。具体而言，在最具挑战性的活动识别任务上，性能提升最为显著，准确率从94.76\%跃升至96.06\%，提升幅度达到1.3个百分点。而在基准准确率本就极高的身份识别和场景识别任务上，加权策略也分别带来了0.12\%和0.07\%的微幅提升，最终分别达到了99.93\%和99.99\%。这一结果直观地证明了合理分配各分支的损失权重对于提升模型整体性能，尤其是攻克困难任务瓶颈具有至关重要的作用。

深入剖析这一现象背后的优化动力学机理，可以发现不同任务之间存在着复杂的竞争与协同关系。在本研究中，场景识别和身份识别相对较为容易，因为WiFi信号中的环境多径特征和人体生物射频特征具有较高的静态稳定性和区分度，模型往往能在训练早期迅速收敛。相比之下，人体活动识别需要捕获复杂的时序动态变化和细粒度的动作模式，属于高难度的“慢收敛”任务。在不使用权重调节（即“否”）的情况下，简单任务的梯度可能在反向传播中占据主导地位，或者由于简单任务过快收敛导致总体损失下降明显，诱导优化器误以为模型已达到理想状态，从而过早降低学习率，导致活动识别分支未能得到充分的训练，陷入次优解。

引入损失函数权重（即“是”）本质上是一种梯度的整流与平衡机制。通过赋予困难任务（活动识别）相对较高的权重，或者动态调整各任务权重的比例，模型被迫在优化过程中给予活动识别分支更多的关注，确保了在保证简单任务精度的同时，持续推动困难任务的特征学习。实验数据中活动准确率的大幅提升正是这一机制生效的直接体现。同时，这种平衡策略也防止了模型在简单任务上过度自信（Over-confidence），在一定程度上起到了正则化的作用，从而在身份和场景任务上也实现了锦上添花的性能提升。综上所述，损失函数权重的合理设置是实现多任务协同进化、避免“木桶效应”的关键策略，对于构建高性能的WICTIVITY系统不可或缺。

\xsubsection{跨域实验结果分析}{}

在人体感知领域，跨域（Cross-domain）泛化能力是衡量算法实用性的核心指标，主要涵盖跨受试者与跨感知环境两个维度。在本研究的初步设计阶段，曾考虑进行跨环境实验以验证模型在不同物理空间下的鲁棒性。然而，不同感知场景（如家庭、实验室、办公室）中所采集的活动类别集合存在显著差异，这种类别标签空间的不一致性使得跨环境实验演变为复杂的零样本学习问题，难以客观评价多模态融合算法本身的域迁移效果。因此，本文后续实验将重点聚焦于跨受试者维度，通过解决由于个体生理差异引发的领域偏移问题，来验证系统的泛化能力。

\subsubsection{跨人实验}{}

跨人实验旨在评估模型在面对未见过的受试者时的性能表现。首先进行的是基准跨人实验，其动机在于量化受试者个体生理特征与运动习惯带来的领域偏移程度。该实验采用了严谨的留一法（Leave-One-Out）交叉验证方案：在 16 名受试者的完整数据集中，每次循环选取 15 名受试者作为源域用于模型预训练，剩余 1 名受试者的全量数据作为目标域用于性能评估。

表3-X详细列出了针对16名不同志愿者（ID 0-15）的测试结果。实验结果显示，在未进行任何领域适配的情况下，WINet 在目标受试者上的平均初始活动识别准确率仅为 72.57\%。相较于受试者内测试（In-subject）近 96\% 的识别精度，这一显著的性能下降证实了人体感知任务中个体特异性对模型决策边界的剧烈冲击，凸显了进行个性化校准与适配的必要性。

\begin{table}[H]
  \centering
  \caption{跨人实验结果}
  \begin{tblr}{
    colspec={X[c] X[c] X[c] X[c]},
  }
  \toprule
  测试志愿者 & 活动准确率 & 身份准确率 & 场景准确率 \\
  \midrule

  0  & 85.32\% & 99.39\% & 16.73\% \\
  1  & 55.14\% & 81.64\% & 11.48\% \\
  2  & 82.56\% & 99.29\% & 15.76\% \\
  3  & 74.60\% & 99.62\% & 1.20\% \\
  4  & 62.13\% & 98.56\% & 29.95\% \\
  5  & 76.29\% & 99.22\% & 0.13\% \\
  6  & 67.01\% & 99.47\% & 2.52\% \\
  7  & 71.91\% & 99.20\% & 0.05\% \\
  8  & 69.87\% & 99.81\% & 32.60\% \\
  9  & 68.72\% & 98.73\% & 11.04\% \\
  10 & 72.71\% & 99.60\% & 1.02\% \\
  11 & 72.47\% & 99.91\% & 1.03\% \\
  12 & 79.33\% & 99.94\% & 1.25\% \\
  13 & 76.55\% & 99.79\% & 2.36\% \\
  14 & 69.43\% & 99.91\% & 8.33\% \\
  15 & 69.26\% & 99.95\% & 41.25\% \\

  \bottomrule
  \end{tblr}
\end{table}

为了评估 3.3.3 节提出的少样本适配策略对上述性能退化的挽救能力，本文开展了跨人少样本适配实验。该实验的动机是验证在仅需用户提供极少量标注数据的前提下，系统能否通过特征空间对齐实现性能的快速恢复。实验方案延续了留一法的基本架构，但在目标受试者的测试过程中，引入了一个包含 $n$ 个样本的支撑集（Support Set），其中 $n$ 即为 Shot 次数，其范围设定为 0 至 240。在适配过程中，系统冻结主干网络，仅微调瓶颈适配器与分类头参数，并引入原型对齐损失以校准特征分布。

\begin{figure}[h]
  \centering
  \includegraphics[width=\textwidth]{跨域少样本适配策略实验结果.png}
  \caption{跨域少样本适配策略实验结果}
\end{figure}

根据实验数据显示，适配策略在极低数据成本下表现出了显著的性能跃升。当每类仅提供 5 个样本（约 2.5 秒数据）进行校准时，准确率由基准的 $72.57\%$ 迅速回升至 $76.95\%$；当样本量增加至 10 个时，准确率进一步突破 $78.82\%$。这一现象揭示了原型对齐机制在超球面几何约束下的高效性：即使样本稀缺，全局语义锚点也能为目标域特征提供正确的漂移修正方向。随着支撑集规模持续扩大至 240 个样本（约 2 分钟数据），准确率最终稳定在 $90.99\%$ 左右。数据轨迹表明，模型在适配前期的提升斜率极大，随后逐渐平缓并趋于饱和。这说明残差结构适配器能够高效捕捉目标受试者的特异性补偿量，仅需极短时间的动作初始化即可完成大部分分布校正。

综上所述，本小节通过对比基准跨人实验与少样本适配实验的结果，完整揭示了领域偏移的影响以及本文策略的有效性。实验证明，虽然个体差异会给预训练模型带来严峻挑战，但通过嵌入轻量化适配器并结合原型引导机制，系统能够以极低的标定成本实现从通用特征到个性化特征的平滑迁移。这种在识别精度与用户负担之间的良好折中，为多模态感知系统在复杂个体间的工程化落地提供了坚实的实验依据。

\xsection{本章小结}{Ref labels}

本章针对复杂室内环境下人体活动感知的鲁棒性与多任务识别需求，提出并验证了基于多模态融合的WICTIVITY模型框架。首先，我们构建了包含IMU惯性数据与WiFi CSI射频信号的异构多模态数据集，并设计了适配多任务学习的数据分割与预处理流程；其次，详细阐述了基于状态空间模型Mamba的双分支特征提取架构与特征级拼接融合策略，有效克服了传统RNN与Transformer在长序列建模中的计算瓶颈。通过一系列系统性的实验分析，包括模态与设备消融、骨干网络对比、标准化策略优选以及跨域泛化评估，全面验证了所提方法在人体活动、身份与场景联合识别任务上的优越性。实验结果表明，该模型不仅充分利用了多模态信息的互补优势实现了高精度的闭集识别，深入揭示了不同传感器节点与信号分量的物理贡献机制，同时也客观量化了模型在面对未见场景与异质用户时的泛化边界，为后续提升开放环境下的普适感知能力奠定了坚实的实证基础。
