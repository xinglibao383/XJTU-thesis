% !TeX root = ../main.tex

\xchapter{相关理论与技术}{Floating: Figures, Tables}

本章将概述本文研究任务所涉及的核心理论与技术，为后续方法设计与实验分析提供基础。内容主要包括两类传感器：惯性测量单元和 WiFi信道状态信息，以及多种深度学习网络模型：ResNet、LSTM与GRU、Transformer和Mamba。同时，本章还将介绍多模态特征融合与对齐的方法，包括交叉注意力机制和对比学习技术，以实现不同模态数据的有效特征表示和融合。

请先注意，在\LaTeX 中，图表不一定要像 Word 一样被固定在某个区域，程序会自动根据上下文为图片选择合适的位置，同时还有具有超链接的交叉引用自动定位图片位置，因此不要强求图片或表格一定处于某个位置。如非要定位，则应在浮动体环境中使用\clist{[H]}选项指定位置。

\begin{tcolorbox}[colback=red!5!white,colframe=red!75!black]
  \begin{enumerate}[leftmargin=0.5cm]
    \item 图、表、公式等一律用阿拉伯数字分章连续编号，如 图1-3、表2-1、（3-2）等。图、表、公式等与正文之间间隔0.5行。

    \item 图应有图题，表应有表题，并分别置于图号和表号之后，图号和图题应置于图下方的居中位置，表号和表题应置于表上方的居中位置。引用图或表应在图题或表题右上角标出文献来源。

    \item 若图或表中有附注，采用英文小写字母顺序编号，附注写在图或表的下方。
  \end{enumerate}
\end{tcolorbox}

\xsection{传感器}{Figures}


\begin{tcolorbox}[colback=red!5!white,colframe=red!75!black]
  \begin{enumerate}[leftmargin=0.5cm]
    \item 插图须紧跟文述。在正文中，一般应先见图号及图的内容后再见图，一般情况下不能提前见图，特殊情况须延后的插图不应跨节；
    \item 提供照片应大小适宜，主题明确，层次清楚，金相照片一定要有比例尺；
    \item 图应具有“自明性”，即只看图、图题和图例，不阅读正文，就可理解图意。图中的标目是说明坐标轴物理意义的项目，它是由物理量的符号或名称和相应的单位组成。物理量的符号由斜体字母标注，单位的符号使用正体字母标注，量与单位间用斜线隔开。例如：$I/\SI{}{A}$，$\rho/\SI{}{kg\cdot m^{-3}}$，$F/\SI{}{N}$，$v/\SI{}{m\cdot s^{-1}}$等等；
    \item 图中用字为五号，如排列过密，用五号字有困难时，可小于五号字，但不得小于七号字；
    \item 图尺寸的\textbf{一般宽高比}应为 \SI{6.67}{cm}$\times$\SI{5.00}{cm}。特殊情况下，也可为 \SI{9.00}{cm}$\times$\SI{6.75}{cm}，或 \SI{13.5}{cm}$\times$\SI{9.00}{cm}。总之，一篇论文中，同类图片的大小应该一致，编排美观、整齐；
    \item 一幅图如有若干幅\textbf{分图}，均应编分图号，用(a)，(b)，(c),...... 按顺序编排，且各分图的分题注直接列在各自分图的正下方，总题注列在所有分图的下方正中「
  \end{enumerate}
\end{tcolorbox}

\xsubsection{惯性测量单元}{Single Figure}

惯性测量单元（Inertial Measurement Unit, IMU）是一种用于测量物体在三维空间中的比力（Specific Force）和角速度（Angular Velocity）的装置，是现代智能可穿戴设备（如智能手表、智能眼镜等）中的核心感知组件 。标准的六轴IMU通常集成了三轴加速度计和三轴陀螺仪，能够实现对物体六自由度（6-DoF）运动状态的实时捕捉。得益于微机电系统（Micro-Electro-Mechanical Systems, MEMS）技术的飞速发展，IMU具备了小型化、低功耗和高集成度的特性，能够将环境运动数据转化为高精度的数字信号 。在人体活动识别（HAR）、健康监测及虚拟现实交互等领域，IMU凭借其非视觉依赖性和隐私友好的特点，为设备提供了精确的运动跟踪与姿态感知能力。

加速度计是一种能够测量物体沿特定轴向线性加速度的传感器。在捷联式惯性导航系统中，加速度计测量的物理量实际上是物体相对于惯性空间的“比力”，即由非引力（如支撑力、推力）产生的加速度，其测量值包含了运动产生的加速度分量与重力加速度分量 。

\subsubsection{加速度计}

加速度计是一种能够测量物体沿特定轴向线性加速度的传感器。在捷联式惯性导航系统中，加速度计测量的物理量实际上是物体相对于惯性空间的“比力”，即由非引力（如支撑力、推力）产生的加速度，其测量值包含了运动产生的加速度分量与重力加速度分量。

现代可穿戴设备中广泛使用的MEMS加速度计通常基于电容感应原理。其微观结构包含一个悬挂在弹簧系统上的微小质量块（Proof Mass）和固定的感应电极。当设备在三维空间（X、Y、Z轴）发生加速运动时，惯性力作用于质量块使其产生相对位移，导致梳齿状电容结构的间距发生改变，从而引起电容值的变化。传感器通过检测这一物理变化并将其转化为电压信号，最终计算出设备在各个轴向上的加速度。

加速度计的测量基础遵循牛顿第二定律 $F=m\cdot a$。对于三轴加速度计，其输出可表示为一个三维向量 $a=[a_{x}, a_{y}, a_{z}]$。考虑到实际应用中的环境因素，其测量模型可表达为：

\begin{equation}
a_{measured} = a_{linear} + R \cdot g + b_a + n_a
\label{eq:acc_model}
\end{equation}

其中，$a_{measured}$ 为传感器的原始测量值，$a_{linear}$ 为物体实际的运动加速度，$R$ 为将重力矢量投影到传感器坐标系的旋转矩阵，$g$ 为重力加速度（通常取 $9.81 \, m/s^2$），$b_a$ 为零偏误差（Bias），$n_a$ 为随机噪声。

加速度计使用站心坐标系或载体坐标系表示信号。在静态或低速运动下，利用重力在三轴上的分量分布，可以精确计算出设备的倾斜角度（如俯仰角 Pitch 和 横滚角 Roll），常用于静态姿态解算。然而，由于无法区分重力加速度与运动加速度，在剧烈运动场景下仅依赖加速度计难以进行精确的位移重构。

\subsubsection{陀螺仪}

陀螺仪是一种用于测量物体围绕各轴旋转的角速度（即单位时间内的角度变化）的传感器，主要用于监测物体的旋转状态和航向变化。

虽然传统机械陀螺仪基于角动量守恒定律，但在可穿戴设备中广泛使用的MEMS陀螺仪主要基于科里奥利力（Coriolis Force）原理。当内部的振动质量块在旋转体系中运动时，会受到一个垂直于旋转轴和振动方向的科里奥利力，该力导致质量块发生微小的横向位移。通过电容检测该位移即可推算出旋转的角速度，进而实现对旋转行为的感知。

角速度 $\omega$ 与物体旋转角度 $\theta$ 的微分关系为 $\omega = d\theta/dt$。三轴陀螺仪的测量输出通常表示为向量 $\omega=[\omega_{x}, \omega_{y}, \omega_{z}]$，单位通常为度/秒（$^{\circ}/s$）或弧度/秒（$rad/s$）。通过对角速度进行时间积分，可以推算出物体相对于初始状态的旋转角度：

\begin{equation}
\theta(t) = \theta(t_0) + \int_{t_0}^{t} \omega(t) \, dt
\label{eq:gyro_integral}
\end{equation}

陀螺仪对快速旋转动作（如手腕转动、头部摆动）具有极高的灵敏度，且不受重力加速度的影响，因此在动态姿态解算中与加速度计形成互补。然而，陀螺仪的测量值存在零偏不稳定性，长时间积分会导致角度误差随时间线性累积，即产生漂移现象。

\subsubsection{误差分析与多模态必要性}

尽管IMU在短时间内具有较高的测量精度，但受限于MEMS工艺，其在长时间运行中面临以下挑战：

\begin{enumerate}
    \item 积分漂移（Drift）：通过对加速度进行二次积分获取位移，或对角速度进行一次积分获取角度时，传感器固有的零偏（Bias）和噪声会被持续放大，导致积分结果随时间呈现发散趋势。
    \item 环境噪声：加速度计对高频振动敏感，且在复杂活动中难以将重力分量与运动分量完全解耦，这在人体运动追踪任务中会引入显著的位置误差。
\end{enumerate}

鉴于上述局限性，在基于多模态融合的人体活动识别与姿态重构研究中，通常不单纯依赖IMU的原始数据积分，而是采用多模态融合技术。例如，引入Wi-Fi CSI信号或视觉信息作为外部观测数据，利用深度神经网络（如Transformer、LSTM）对IMU的时序特征进行建模与误差补偿，从而实现长时间、高精度的行为识别与姿态估计。

\begin{texcodeonly}[]{}
  \begin{figure}[h]
    \centering
    \includegraphics[width=.5\textwidth]{device_ablation_experiment.pdf}
    \caption{校徽}
  \end{figure}
\end{texcodeonly}

\begin{figure}[h]
  \centering
  \includegraphics[width=.8\textwidth]{device_ablation_experiment.pdf}
  \caption{校徽}
\end{figure}

一些图片下方的文字。一些图片下方的文字。一些图片下方的文字。一些图片下方的文字。一些图片下方的文字。一些图片下方的文字。一些图片下方的文字。一些图片下方的文字。一些图片下方的文字。一些图片下方的文字。一些图片下方的文字。一些图片下方的文字。一些图片下方的文字。

\xsubsection{WiFi信道状态信息}{Use subcaption instead of subfigure}

随着无线通信技术的演进，Wi-Fi（Wireless Fidelity）已不仅仅是局域网数据传输的标准，更逐渐演变为一种非接触式的环境感知手段。本节将详细阐述支撑Wi-Fi感知的核心技术——正交频分复用（OFDM）以及描述无线信道细粒度特征的信道状态信息（CSI）。

\subsubsection{正交频分复用技术 (OFDM)}

正交频分复用（Orthogonal Frequency Division Multiplexing, OFDM）是现代Wi-Fi标准（如IEEE 802.11n/ac/ax）的核心调制技术，其主要目的是在多径传播环境下实现高效、可靠的高速数据传输。

OFDM 将高速串行数据流分解为多个低速并行子数据流，并将它们调制到一组相互正交的子载波（Sub-carriers）上进行传输。与传统的频分复用（FDM）不同，OFDM 允许子载波的频谱相互重叠，但通过保持载波间的正交性，接收端可以利用快速傅里叶变换（FFT）无干扰地分离出各个子载波的信号。

从数学角度看，OFDM 信号可以表示为多个子载波的线性叠加。设 $N$ 为子载波数量，$f_0$ 为子载波间隔，则时域上的发送信号 $s(t)$ 可表示为：

\begin{equation}
s(t)=\sum_{k=0}^{N-1}X_{k}\cdot e^{j2\pi kf_{0}t}
\end{equation}

其中，$X_k$ 表示第 $k$ 个子载波上的复数调制符号（如QAM符号），$j$ 为虚数单位。

OFDM 技术将宽带频率选择性衰落信道转化为多个平坦衰落的窄带子信道，这种机制不仅极大地提高了频谱利用率，还显著增强了系统对抗多径效应（Multipath Effect）和符号间干扰（ISI）的鲁棒性，为提取高质量的信道特征提供了物理层基础。

\subsubsection{信道状态信息 (Channel State Information, CSI)}

传统的无线感知常依赖接收信号强度指示（RSSI），但这是一种叠加了多径信号的粗粒度特征，稳定性较差。相比之下，信道状态信息（CSI）属于物理层（PHY Layer）特征，它从子载波层面细粒度地描述了无线信号在发射端与接收端之间的传播特性，反映了信号在传输过程中的散射、反射、衰减及相位偏移等物理现象。

在无线通信系统中，接收信号 $Y$、发送信号 $X$ 与信道脉冲响应 $H$ 以及噪声 $N$ 之间的关系通常描述为 $Y = H \cdot X + N$。CSI 即为信道频率响应（Channel Frequency Response, CFR）的采样值，也就是矩阵 $H$ 的估计。

假设无线通信系统包含 $M$ 个发射天线和 $N$ 个接收天线，则信道状态信息矩阵 $H$ 可表示为：

\begin{equation}
H=\begin{bmatrix}h_{11}&h_{12}&\cdots&h_{1N}\\ h_{21}&h_{22}&\cdots&h_{2N}\\ \vdots&\vdots&\ddots&\vdots\\ h_{M1}&h_{M2}&\cdots&h_{MN}\end{bmatrix}
\end{equation}

其中，矩阵元素 $h_{mn}$ 表示从第 $m$ 个发射天线到第 $n$ 个接收天线的复数信道增益。

CSI 矩阵中的每一个元素 $h_{mn}$ 均为复数，包含了信号的振幅（Amplitude）和相位（Phase）信息，可表示为 $h_{mn} = |h_{mn}|e^{j\theta_{mn}}$。

\begin{itemize}
    \item 振幅信息：反映了信号在特定子载波上的能量衰减程度。其计算公式为：
    \begin{equation}
    |\alpha_{mn}|=|h_{mn}|=\sqrt{Re(h_{mn})^{2}+Im(h_{mn})^{2}}
    \end{equation}
    其中，$Re(\cdot)$ 和 $Im(\cdot)$ 分别代表复数的实部和虚部。

    \item 相位信息：反映了信号传播的时延和路径变化，对位置移动极为敏感。其计算公式为：
    \begin{equation}
    \theta_{mn}=\arg(h_{mn})=\arctan\left(\frac{Im(h_{mn})}{Re(h_{mn})}\right)
    \end{equation}
\end{itemize}

Wi-Fi 感知的核心在于“多径效应”引起的CSI波动。当环境中的目标（如人体）发生移动或动作时，会改变信号的传播路径（反射、散射），导致接收端特定子载波的CSI振幅和相位发生特异性变化。通过在时域上分析这些波动模式，并结合机器学习算法，可以实现对呼吸监测、步态识别、跌倒检测及精细动作分类的高精度感知。

相较于由于多径叠加而导致信息模糊的 RSSI，CSI 提供了频域上的多维信息，能够更本质地刻画环境与目标的特征，因此在复杂室内环境下的姿态重构与动作识别任务中表现出更高的稳定性和准确性。

\subsubsection{CSI 相位误差消除与清洗}

虽然 CSI 相位信息包含着对物体运动极为敏感的时空特征，但在商用 Wi-Fi 设备（如 Intel 5300 或 ESP32）上直接获取的原始相位数据往往由于硬件缺陷而无法直接使用。主要的误差来源包括载波频率偏移（Carrier Frequency Offset, CFO）、采样频率偏移（Sampling Frequency Offset, SFO）以及包检测延迟（Packet Detection Delay, PDD）。

1) 原始相位误差模型

考虑到上述硬件非理想特性，第 $k$ 个子载波上的测量相位 $\hat{\phi}_k$ 与真实相位 $\phi_k$ 之间的关系通常建模为：

\begin{equation}
\hat{\phi}_k = \phi_k + 2\pi \frac{k}{N} \delta + \beta + Z
\end{equation}

其中，$N$ 为FFT点数（子载波总数），$\delta$ 为由于采样时钟不同步引起的定时偏移（Timing Offset），$\beta$ 为由于中心频率不同步引起的相位偏移，$Z$ 为测量噪声。由于 $\delta$ 和 $\beta$ 在每个数据包中都是随机变化的，导致原始相位在 $[-\pi, \pi]$ 之间呈现杂乱无章的分布。

2) 线性变换清洗算法

为了恢复可用的相位信息，通常采用线性拟合的方法来消除 $\delta$ 和 $\beta$ 的影响。通过对原始相位进行线性变换（Linear Transformation），可以移除线性误差项。清洗后的相位 $\tilde{\phi}_k$ 计算如下：

\begin{equation}
\tilde{\phi}_k = \hat{\phi}_k - \alpha k - \gamma
\end{equation}

其中，$\alpha$ 和 $\gamma$ 分别是相位随子载波索引变化的斜率和截距，通常通过最小二乘法估计得出。经过清洗后的相位数据能够展现出随时间变化的连续性，从而有效地用于后续的特征提取与动作识别。

\subsubsection{菲涅尔区感知模型}

菲涅尔区（Fresnel Zone）模型是理解 Wi-Fi 感知物理机制的理论基础，它解释了物体位置变化如何导致接收信号强度的波动。

在收发设备之间的视距（LOS）路径周围，存在一系列以收发天线为焦点的同心椭球区域，称为菲涅尔区。对于波长为 $\lambda$ 的信号，第 $n$ 个菲涅尔区边界上的点 $P$ 满足路径差条件：

\begin{equation}
|d(Tx, P) + d(P, Rx) - d(Tx, Rx)| = \frac{n\lambda}{2}
\end{equation}

其中，$Tx$ 和 $Rx$ 分别代表发射端和接收端。

当目标物体穿越这些菲涅尔区时，反射信号的路径长度会发生改变，从而改变反射信号与直射信号（LOS）之间的相位差。

\begin{itemize}
    \item 相干增强：当反射信号与直射信号相位一致时，信号叠加增强。
    \item 相干相消：当两者相位相反时，信号叠加减弱。
\end{itemize}

因此，人体的连续运动会导致接收信号在波峰和波谷之间交替变化，形成类似正弦波的波动模式。通过分析这种波动的频率（对应运动速度）和幅度（对应反射面积），可以反推目标的运动状态。

\subsubsection{多径信号的特征提取}

原始的 CSI 数据流是一个包含时间、频率（子载波）和空间（天线）维度的高维张量。为了输入到深度学习模型中，通常需要进行特征工程处理。

多普勒频移 (Doppler Frequency Shift, DFS)：人体运动会引起无线信号的多普勒效应。DFS 特征反映了目标的运动速度，可以通过对 CSI 时间序列进行短时傅里叶变换（STFT）或离散小波变换（DWT）获得时频图（Spectrogram）。时频图中的能量分布能够清晰地刻画出动作的起止时间、速度变化及周期性特征。

动态分量分离：在室内环境中，CSI 信号包含来自墙壁、家具等静止物体的静态分量（Static Component）和来自运动人体的动态分量（Dynamic Component）。为了提高信噪比，常采用 Hampel 滤波器去除离群值，并利用 主成分分析 (PCA) 或 巴特沃斯高通滤波器 滤除直流分量和低频背景噪声，从而提取出纯净的人体运动特征。

\begin{texcodeonly}[]{}
  \begin{figure}[h]
    \begin{subfigure}[b]{0.49\linewidth}
        \centering
        \includegraphics[width=.5\textwidth]{xjtu.pdf}
        \subcaption{校名}
    \end{subfigure}
    \begin{subfigure}[b]{0.49\linewidth}
        \centering
        \includegraphics[width=.5\textwidth]{xjtu.pdf}
        \subcaption{校名}
        \label{subfigure:icon}
    \end{subfigure}
    \caption{校徽}
    \label{fig:icon}
  \end{figure}
\end{texcodeonly}

\begin{figure}[h]
  \begin{subfigure}[b]{0.49\linewidth}
      \centering
      \includegraphics[width=.5\textwidth]{xjtu.pdf}
      \subcaption{校名}
  \end{subfigure}
  \begin{subfigure}[b]{0.49\linewidth}
      \centering
      \includegraphics[width=.5\textwidth]{xjtu.pdf}
      \subcaption{校名}
      \label{subfigure:icon}
  \end{subfigure}
  \caption{校徽}
  \label{figure:icon}
\end{figure}

% \xsubsection{多张子图分页}{Break multiple figures}

\newcommand{\subfigg}{
  \begin{subfigure}[b]{0.49\linewidth}
    \centering
    \includegraphics[width=.5\textwidth]{xjtu.pdf}
    \subcaption{校名}
  \end{subfigure}
}

为了简化叙述，定义一个命令 \clist{\subfigg} 代表插入一个子图：
\begin{texcodeonly}[]{}
  \newcommand{\subfigg}{
    \begin{subfigure}[b]{0.49\linewidth}
      \centering
      \includegraphics[width=.5\textwidth]{xjtu.pdf}
      \subcaption{校名}
    \end{subfigure}
  }
\end{texcodeonly}

在\clist[columbiablue]{subfig}宏包中提供了\clist{\ContinuedFloat}命令，使一个浮动体环境与其内子图的编号跟随前一个子图，此时两个浮动体环境可以分别设置标题，若具有相同的标题，或后续图的标题设置为 \clist{（续）}，则可以实现子图分页的效果。同时，模板提供了 \clist{\floatcontinue} ，通过包装\clist{\ContinuedFloat}使得子图在此命令处断页，不产生图标题，并在下一页生成图题。此两种命令均可使用，按照喜欢或需求使用。
\clearpage
\subsubsection{使用 ContinuedFloat}

\begin{texcodeonly}[]{}
  \begin{figure}[h]
    \subfigg    \subfigg
    \caption{校徽}
  \end{figure}
  
  \begin{figure}[H]
    \ContinuedFloat
    \subfigg    \subfigg
    \subfigg    \subfigg
    \caption{校徽} % 或 \caption{（续）}
  \end{figure}
\end{texcodeonly}

\begin{figure}[h]
  \subfigg    \subfigg
  \caption{校徽}
\end{figure}

\begin{figure}[H]
  \ContinuedFloat
  \subfigg    \subfigg
  \subfigg    \subfigg
  \caption{校徽} % 或 \caption{（续）}
\end{figure}

\subsubsection{使用 floatcontinue}

\begin{texcodeonly}[]{}
  \begin{figure}[h]
    \subfigg    \subfigg
    \floatcontinue{tb}
    \subfigg    \subfigg
    \subfigg    \subfigg
    \caption{校徽}
  \end{figure}
\end{texcodeonly}

\begin{figure}[h]
  \subfigg    \subfigg
  \floatcontinue{tb}
  \subfigg    \subfigg
  \subfigg    \subfigg
  \caption{校徽}
\end{figure}


\clearpage

\xsection{深度学习网络}{Tables}

\begin{tcolorbox}[colback=red!5!white,colframe=red!75!black]
  \begin{enumerate}[leftmargin=0.5cm]
    \item 如某个表需要转页接排，在随后的各页上应重复表的编号。编号后跟表题（可省略）和“（续）”，如表 1-1（续），续表均应重复表头和关于单位的陈述。表格的设计应紧跟文述。表的编排一般是内容和测试项目由左至右横读，数据依序竖读，应有自明性。若为大表或作为工具使用的表格，可作为附表在附录中给出，论文中的表格参数应标明量和单位的符号；
    \item 表中各物理量及量纲均按国际标准(SI) 及国家规定的法定符号和法定计量单位标注；
    \item 一律使用三线表，与文字齐宽，顶线与底线线粗为 $1.5$ pt，中线线粗是 $1$ pt；
    \item 使用他人表格须注明出处。
    \item 表中用字为五号字体。如排列过密，用五号字有困难时，可小于五号字，但不小于七号。
    \item 表格必须通栏，即表格宽度与正文版面平齐。
  \end{enumerate}
\end{tcolorbox}


\xsubsection{ResNet}{Normal Tables}


考虑到使用成本、内容与样式分离。模板默认调用现代化的表格绘制宏包 \clist[columbiablue]{tabularray}。该宏包的使用方法请使用 \clist{texdoc tabularray}，网上也有此手册的中文翻译。请确保使用的发行版包含这一宏包（即更新至 2023 及以后的版本）。

非跨页表格应使用 tblr 环境制作表格，使用 table 作为浮动体环境，使用 \clist{\toprule}，\clist{\midrule} 和 \clist{\bottomrule} 指定三线表的三条线：

\begin{texcode}[]{}
    \begin{table}[H]
        \caption{tblr 基本用法}
        \label{tbl:2-1}
        \begin{tblr}{
            % colspec={X[r,m,2]X[c,3]X[l,1]}
            X[r,m,2]X[c,3]X[l,1]
        }
            \toprule
            Head & Test & 测试 \\
            \midrule
            Class that has many accessor methods and accesses a lot of external data & ATFD is more than a few & 3 \\
            1 & 2 & 3 \\
            1 & 2 & 3 \\
            \bottomrule
        \end{tblr}
    \end{table}
\end{texcode}

tblr 环境各列的对齐方式可以以键值对的方式在必选参数中设置，或者直接作为唯一必选参数（参考 tabularray 手册第 24 页）。列类型包括 c/l/r, t/b/m, X 等。c/l/r 为默认的三种对齐方式，t/b/m 指定单元格内容超过列宽时垂直方向的对齐方式， X 类型会自动调整列宽使全表宽度与页宽一致，X 类型列的水平、垂直对齐方式以类似 X[c] 的方式在 X 后的方括号内指定。X 类型后的方括号可以指定此列宽度与其他X类列宽度的相对大小，也可使用 \clist{wd=A}直接指定列宽。推荐使用 X 作为列类型。

\begin{texcode}[]{}
    \begin{tblr}{
        colspec={|ll|X[c]|rr|},
        cell{1}{1}={r=3,c=2}{r,m},
        cell{1}{3}={r=2,c=3}{l,t},
        cell{3}{Z}={}{red},
        hline{4-6}={2pt,blue},
    }
        \hline
        1-1 & 1-2 & 1-3 & 1-4 & 1-5 \\
        \hline
        2-1 & 2-2 & 2-3 & 2-4 & 2-5 \\
        \hline
        3-1 & 3-2 & MIDDLE & 3-4 & 3-5 \\
        % \hline (4th)
        4-1 & 4-2 & 4-3 & 4-4 & 4-5 \\
        % \hline (5th)
        5-1 & 5-2 & 5-3 & 5-4 & 5-5 \\
        % \hline (6th)
    \end{tblr}
\end{texcode}

tabularray 的优点在于格式和内容分离，使用更贴合人类习惯。单元格的合并，文字、背景、边框的样式都可以放在参数中设置。 \clist{cell{R}{C}={merge}{style}}用于设置单元格合并，其中 R,C 指定合并单元格左上角的位置，merge参数指定合并多少行多少列，style 调整合并后的单元格样式，其中 R,C 除了使用行号进行索引，可以使用 X,Y,Z 索引倒数第三、二、一行/列，可以使用 odd/even 选择 奇/偶列，还可以使用连字符(dash)选择区间，非常方便。

\xsubsection{LSTM和GRU}{Complicated tables}

在三线表中可以加辅助线，以适应较复杂表格的需要，具体使用方法请自行阅读宏包：

\begin{texcode}[]{}
\begin{table}[H]
  \centering
  \caption{compare with other approachs}
  \label{table:methodcompare}
  \begin{tblr}{
      colspec={*{7}{X[c]}},
      cell{1}{1}={r=2}{c,m},
      cell{1}{2,5}={c=3}{c,m},
      hline{2,4,6}={1pt},
      hline{1,3,Z}={1.5pt},
      row{4,5}={rowsep=0pt},
      row{Z}={m,font=\bfseries},
  }
    Model & trigger inditification & & & Event Extraction & & \\
    & P(\%) & R(\%) & F1(\%) & P(\%) & R(\%) & F1(\%) \\
    Baseline1 & 76.84 & 76.84 & 76.84 & 76.84 & 76.84 & 76.84 \\
    Baseline2  & 76.84 & 76.84 & 76.84 & 76.84 & 76.84 & 76.84 \\
    Baseline3  & 76.84 & 76.84 & 76.84 & 76.84 & 76.84 & 76.84 \\
    Our approach  & 76.84 & 76.84 & 76.84 & 76.84 & 76.84 & 76.84 \\
  \end{tblr}
\end{table}
\end{texcode}

\xsubsection{Transformer}{Long Tables}

\begin{figure}[h]
  \centering
  \includegraphics[width=.5\textwidth]{transformer.png}
  \caption{transformer}
\end{figure}

当表格过长时，使用\clist{longtblr}环境，此时不需要浮动体环境。在可选参数中需要设置\clist{theme=xjtu_std}，以调用预先设置好的续表样式，同时设置表格标题、label、脚注等，具体使用方法请自行阅读宏包。

\begin{texcodeonly}[]{}
    \begin{longtblr}[
        theme = xjtu_std,
        caption = {A Long Long Long Long Long Long Long Table },
        entry = {Short Caption},
        label = {tblr:test},
        note{a} = {脚注},
        note{$\dag$} = {还是脚注},
        remark{Note} = {注释，即表格内没有标注符号的脚注},
        remark{Source} = {还是注释},
    ]{
        colspec = {XX[c]X},
        rowhead = 1, rowfoot = 0,
    }
    \toprule
    Head & Head & Head \\
    \midrule
    Epsilon & Zeta\TblrNote{a} & Eta \\
    Iota & Kappa\TblrNote{$\dag$} & Lambda \\
    Nu & Xi & Omicron \\
    \dots 
    Foot & Foot & Foot \\
    \bottomrule
    \end{longtblr}
\end{texcodeonly}

\begin{longtblr}[
        theme = xjtu_std,
        caption = {A Long Long Long Long Long Long Long Table },
        entry = {Short Caption},
        label = {tblr:test},
        note{a} = {脚注},
        note{$\dag$} = {还是脚注},
        remark{Note} = {注释，即表格内没有标注符号的脚注},
        remark{Source} = {还是注释},
    ]{
        colspec = {XX[c]X},
        rowhead = 1, rowfoot = 0,
    }
    \toprule
    Head & Head & Head \\
    \midrule
    Head & Head & Head \\
    Alpha & Beta & Gamma \\
    Epsilon & Zeta\TblrNote{a} & Eta \\
    Iota & Kappa\TblrNote{$\dag$} & Lambda \\
    Nu & Xi & Omicron \\
    Rho & Sigma & Tau \\
    Phi & Chi & Psi \\
    %
    % ......
    Alpha & Beta & Gamma \\
    Epsilon & Zeta & Eta \\
    Iota & Kappa & Lambda \\
    Nu & Xi & Omicron \\
    Rho & Sigma & Tau \\
    Phi & Chi & Psi \\
    % Foot & Foot & Foot \\
    Alpha & Beta & Gamma \\
    \midrule
    Epsilon & Zeta & Eta \\
    Iota & Kappa & Lambda \\
    Nu & Xi & Omicron \\
    Rho & Sigma & Tau \\
    Phi & Chi & Psi \\
    Foot & Foot & Foot \\
    Alpha & Beta & Gamma \\
    Epsilon & Zeta & Eta \\
    \midrule
    Iota & Kappa & Lambda \\
    Nu & Xi & Omicron \\
    Rho & Sigma & Tau \\
    Phi & Chi & Psi \\
    Foot & Foot & Foot \\
    Alpha & Beta & Gamma \\
    Epsilon & Zeta & Eta \\
    Iota & Kappa & Lambda \\
    Nu & Xi & Omicron \\
    Rho & Sigma & Tau \\
    Phi & Chi & Psi \\
    Foot & Foot & Foot \\
    \bottomrule
\end{longtblr}

\xsubsection{Mamba}{Long Tables}

\xsection{多模态特征融合与对齐}{Figures}

\xsubsection{交叉注意力机制}{Long Tables}

\xsubsection{对比学习}{Long Tables}